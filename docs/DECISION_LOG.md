# JARVIS — DECISION LOG

The canonical record of every product and technical decision, chronological, append-only. Entries are never edited; a superseded decision gets a new entry marking the old one as superseded and naming the entry that replaced it.

Status key: **Active** · **Superseded** · **Deferred** · **Rejected**

---

## 2026-08-27 — Product discovery

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-001 | Product Bible opened; no code written before product definition is frozen | Brief §17, §20. Premium coding credits are a scarce budget and must not be spent on architecture discovery | Active |
| D-002 | Instrument: GBP/USD | The project's framing is session-structural, and GBP's London character is the most pronounced among the majors. Range-to-cost ratio comparable to EUR/USD despite wider spreads. EUR/USD retained as the fallback if the Stage 0 gate fails | Active |
| D-003 | Environment: Windows, native Python 3.12, local only, no cloud | Single user, ~150 MB working analytical dataset. Cloud adds ops burden and weakens the vault's credibility. Native over WSL to avoid a filesystem boundary during multi-gigabyte ingest | Active |
| D-004 | AI layer excluded from V1 entirely | An intelligence layer has nothing to say until hundreds of experiments exist. It is the highest-cost, least-evidenced component, and it creates exactly the dependence the human-outcome objective forbids. A chat interface covers teaching and criticism at zero engineering cost | Active |
| D-005 | Holdout: 2023–present sealed, one irreversible unlock per hypothesis family, descriptive access permitted | A shorter seal yields too few events to be meaningful. Descriptive access removes the recent-regime blind spot at near-zero cost to test integrity | **Superseded in part by D-014** (scope of descriptive access narrowed) |
| D-006 | V1 exclusion list ratified: no live trading, ML, funded modelling, multi-pair, replay trainer, web UI, auto-optimisation | Each excluded item either costs weeks with no research value or actively increases the probability of forming a false belief | Active |
| D-007 | Roadmap reordered: market description precedes strategy machinery | The user is a blank slate. A backtester with no hypotheses to test is premature, and imported hypotheses are someone else's noise | Active |
| D-008 | Product definition frozen (Product Bible v1.0) | All twelve critical decisions resolved; the pre-registration ritual deliberately deferred to Stage 3 | Superseded by D-019 (v1.1) |

---

## 2026-08-28 — Technical specification

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-009 | Technical Bible produced in four parts (A–Z) plus WP-000 | Implementation-grade specification so the coding model never invents architecture, infers semantics, or makes research-integrity decisions while coding | Active |
| D-010 | Architecture: five strict layers, enforced by `import-linter` in CI | Two rules carry the integrity load — the single-reader rule (only `bars` and `vault` touch market Parquet) and the purity rule (`features`/`strategies`/`opportunities` cannot import results modules). Both must be machine-enforced or they decay within months | Active |
| D-011 | `pre_london` session defined in `Europe/London` local time, not Tokyo local | Defining the Asian range in Tokyo time makes its distance from the London open swing by an hour twice a year in each of two zones, creating four clerical regimes per year. Defining it in London time fixes its relationship to the thing it is measured against. `tokyo` remains available separately | Active |
| D-012 | Vault not encrypted at rest | The only adversary is the user. Encryption adds a key-loss failure mode that could destroy the holdout permanently, without adding protection. The vault's force is the `GatedReader` and the permanent unlock record | Active |
| D-013 | Reports: Markdown plus Parquet/CSV sidecars. No HTML generator, in V1 or likely ever | Reports are read by one person, in a terminal or notebook, need to be diffable, and need to be pasteable into a chat window — which is now the primary AI route since D-004. Markdown satisfies all four; HTML satisfies none better and costs a rendering pipeline | Active |
| D-014 | Event calendar: static curated CSV, versioned as a dataset, used as an exclusion flag and robustness segmentation only — never as a predictive feature in V1 | Preserves the offline-by-default property. No scraping, no API dependency | Active |
| D-015 | Scan kept as a per-bar Python callable rather than vectorised | 10–30 minutes for a full-history run is acceptable for a tool run a few times a day. Vectorising would trade the project's core value — the user understanding their own strategy — for speed nobody needs. Escape hatch: restrict scanning to the session window, a 10-line change | Active |
| D-016 | Primary metric for pre-registration restricted to `expectancy_r`; win rate may be reported but not pre-registered | For a fixed-R-multiple strategy, win rate and expectancy are nearly the same statistic, and win rate invites the wrong intuition | Active |
| D-017 | Maximum drawdown never reported as a point estimate | It is an extremum statistic with a wide right-skewed sampling distribution that grows with `n`. Always reported alongside its bootstrap distribution, with the 95th percentile as the plannable number | Active |
| D-018 | Sharpe ratio excluded | Trade-level Sharpe has no meaningful time normalisation and depends on trade frequency, so it is not comparable across strategies | Active |

### Adversarial review changes (within PM authority; recorded for visibility)

| # | Decision | Rationale | Status |
|---|---|---|---|
| AR-1 | Stage 2 descriptive reports run on 2007–2014. Every development report must show 2015–2018 as a separate segment labelled "period not seen during hypothesis generation." An effect absent in that segment cannot reach PROMISING | The development set is otherwise used twice — to generate the hypothesis and to test it. This is the largest single source of false evidence in the design and pre-registration cannot fix it, because pre-registration happens after the observation | Active |
| AR-2 | Report three intervals, not two: unadjusted, Šidák over distinct sweep IDs, and Šidák over all family runs. The evidence tier uses the conservative bound | Correcting properly for correlated tests requires assumptions this project cannot justify. Showing the bracket is more honest than picking one point inside it | Active |
| AR-3 | The 1.5× spread-stressed result is a co-equal headline figure on every experiment report, labelled "retail-realistic estimate" beside the base case labelled "institutional feed" | Dukascopy is an aggregated institutional feed. Retail spreads are materially wider, especially in the first minutes of the London open — precisely the target window | Active |
| AR-4 | Event calendar promoted from deferred to a Stage 2 deliverable | Without it, strategies may be harvesting scheduled-release moves that are untradeable in practice because spreads blow out | Active |
| AR-5 | Vault gating enforced by capability token, not call-stack inspection. `GatedReader` constructors are private; readers are minted only by `RunContext.reader(query_class)` | Stack inspection is fragile, hard to test, and breaks under any refactor adding a wrapper. A capability token makes the vault boundary a type-level property | Active |
| AR-6 | Stage 2 ships three reports (R1, R2, R5). R3, R4, R6 deferred to Stage 2F, built only after the first three have been used and found wanting. Robustness ships six mandatory tests at 5E, the rest at 5G | Six reports and twelve robustness tests before anything has been read or any strategy exists is speculative build on the critical path | Active |

---

## 2026-08-28 — Amendment ratification

All six proposed amendments ratified by the user without modification.

| # | Amendment | Decision | Rationale | Status |
|---|---|---|---|---|
| D-019 | **PDLA-01 — Stage 0 sequencing** | **RATIFIED.** Stage 0 redefined as a vertical slice (milestones 0A–0I): scaffold, fetcher, parser, time engine, session engine, resampler, QA, five features, probe. Stage 1 redefined as *completing* the data layer rather than building it | Stage 0 as originally sequenced required Stage 1's deliverables and could not be built. Any shortcut avoiding the dependency makes the probe untrustworthy — counting Asian-range events without a DST-correct session engine gives wrong counts for roughly four weeks a year, and the probe's entire output is a count. **User ruling: correctness of the feasibility gate takes priority over preserving the original two-day estimate.** Accepted cost: three premium milestones and two to three weeks | Active |
| D-020 | **PDLA-02 — Edge definition** | **RATIFIED.** An edge is a positive expectancy in R, already net of modelled costs, whose stationary-bootstrap 95% lower bound exceeds zero, surviving year-by-year segmentation, exceeding its placebo baseline, and remaining above zero under 1.5× cost and slippage stress | The original wording subtracted costs twice, since spread is inside the fill prices and commission and slippage are inside `r_net`. A stress requirement serves the original intent — margin above costs — without the double subtraction | Active |
| D-021 | **PDLA-03 — Vault access scope** | **RATIFIED.** Vault reads restricted to a fixed enumerated set of coarse annual aggregates: annual median and IQR of session ranges, realised volatility and spread, plus admissible day counts. One row per year per session. No conditioning, no cross-tabs, no sub-daily breakdown. Stage 2 conditional reports and Stage 0 both run on 2007–2022 only | Conditional distributions over vault years fit the hypothesis to the holdout through the user's own reasoning — the one contamination path no software control can detect afterwards. Coarse aggregates answer the calibration question ("is the market now like the market I studied") without suggesting a strategy | Active; supersedes part of D-005 |
| D-022 | **PDLA-04 — Spread charged once** | **RATIFIED.** Spread is charged solely by the bid/ask price convention. No additive spread cost. The manifest validator rejects any spread cost field. The empirical spread distribution is retained as a descriptive output and as the scaling basis for slippage | Storing real bid/ask and additionally modelling a spread cost charges it twice, roughly doubling cost drag and rejecting real effects | Active |
| D-023 | **PDLA-05 — R denominator** | **RATIFIED.** `r_denominator = |entry_reference_price − stop_reference_price|`, captured at signal time on the opportunity row, copied to the trade, never recomputed from actual fills | Using the actual fill lets entry slippage widen the denominator, shrinking reported R losses — an error that makes a strategy look better precisely when execution was worst | Active |
| D-024 | **PDLA-06 — Two-tier frequency gate** | **RATIFIED.** `PROCEED GBP/USD` requires the narrowest measured context intersection to have a median annual count ≥ 100 **and** a 10th-percentile annual count ≥ 60 over admissible years in 2007–2022. Counts 40–100 trigger one widening attempt. Below 40 after widening, the EUR/USD comparison runs | The original gate did not say what was being counted. Context frequency and final setup frequency differ by two to four times, so a 40/year gate on contexts would pass specialisations capable of only 10–20 tradeable setups a year — never enough for a usable holdout sample | Active |
| D-024a | **Qualification on D-024, recorded at the user's explicit instruction** | The thresholds of 100 (median) and 60 (P10) are a **conservative research-design choice for V1, not a universal statistical law.** They encode an assumption that a directional filter and entry trigger remove roughly 50–75% of context instances, and a judgement that ~150 holdout events is the minimum at which a small effect is distinguishable from zero. Both are defensible; neither is proven. **They may be changed only through this decision log and only on the basis of evidence** — for example measured context-to-setup attrition once Stage 3 exists, or a power calculation against an observed effect size. **They may not be changed because a probe failed to clear them.** | Prevents the gate from being quietly relaxed at the moment it becomes inconvenient, which is the moment it matters | Active |
| D-025 | **Product Bible v1.1 issued; status set to READY TO BUILD STAGE 0** | Supersedes v1.0. All six amendments incorporated; all other v1.0 decisions carried forward unchanged | Both blockers resolved | Active |
| D-026 | **WP-000 released for implementation. WP-001 onward deliberately not written** | User ruling: implement and review WP-000 first so the work-package format and the review gate are tested on one small package before the pattern is multiplied across forty-three more. A defect in the template would otherwise be replicated forty-four times | Active |

---

## 2026-08-28 — WP-000 and WP-001 review outcomes

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-027 | **WP-000 (repository scaffold) — APPROVED**, after one correction round | Initial submission inferred a linear-pipeline `.importlinter` contract because the Technical Bible wasn't attached to the agent's context (a process gap, not a coding failure). Corrected against the real five-layer model plus named forbidden-dependency contracts; verified independently by reading the actual `.importlinter` file rather than a narrative summary | Active |
| D-028 | **Process change: all review from this point reads the actual repository (via clone or fetch), never a narrative summary of test results** | Established after D-027 required raw `pytest`/`lint-imports` output before approval could be trusted. Repository access (public GitHub) makes this practical going forward | Active |
| D-029 | **WP-001 (Dukascopy fetcher) — REJECTED, then APPROVED after correction 2c13a4b** | URL construction, empty/missing/404 classification, and resumability were correct on first submission. One real defect: at the default `concurrency=4`, an exception inside a `threading.Thread` worker (e.g. a disk-write failure) was silently swallowed by Python's thread model — `ingest_range` returned a clean, fully-empty `IngestReport` rather than raising, meaning a HALT condition would have reported as success. Reproduced independently (all four workers died, zero hours accounted for, no exception, would exit 0) before requesting correction. Fixed by replacing manual `threading.Thread` bookkeeping with `concurrent.futures.ThreadPoolExecutor` and calling `future.result()` per future, which re-raises deterministically. Re-verified independently with the same reproduction: `IntegrityError` now propagates correctly and the CLI exits 2 | Active |
| D-029a | **Standing note for Stage 4 (backtester)**: the WP-001 defect (a threaded worker silently swallowing an exception that should HALT the run) is a pattern worth checking for explicitly wherever Stage 4's execution simulation introduces concurrency, since the consequence there — a corrupted or incomplete backtest reporting as a clean result — is exactly the failure mode the whole project exists to prevent | Recorded for future review, not yet applicable | Deferred |
| D-030 | **WP-002 (Dukascopy `.bi5` parser) — APPROVED**, first submission, no correction needed | Byte-order unpack independently verified against Part 1 §D.1's offset table (bid/ask assignment checked specifically, since a swap would silently invert every price without necessarily failing a self-consistent test). Golden fixture's raw bytes independently decoded outside `parse_bi5` and confirmed to match the test's hand-written literals exactly, including the same-millisecond tie-break pair. Empty-vs-corrupt distinction verified both by the submitted tests and by an additional truncated-real-file reproduction (a more realistic corruption mode than pure garbage bytes), which correctly raised `IntegrityError` | Active |
| D-030a | **Dependency debt: `test_property_n_records_round_trip` uses stdlib `random` with fixed seeds, not the `hypothesis` package** | WP-002 contained a genuine internal conflict — it named the test "Hypothesis-based" per Technical Bible Part 4 §S.2's intended tooling, while also explicitly forbidding any new dependency and not granting `pyproject.toml` as editable. Correctly resolved by honoring the binding constraint (no dependency) and flagging the gap rather than silently picking one reading. A future package that is explicitly granted `pyproject.toml` should add `hypothesis` and convert this test to a real property test with shrinking, rather than fixed-seed pseudo-randomness, which cannot shrink a failing case to a minimal reproduction | Deferred — revisit when a package touching `pyproject.toml` is next in scope |
| D-031 | **Technical Bible correction: §E.7 test T1 and §M/SM-13 stated an inverted DST expected value** | Both read "London 08:00 maps to 03:00 New York, not 04:00" for the US/EU divergence window. This is backwards — during divergence the difference narrows to 4h, so London 08:00 → **04:00** NY, and 03:00 is the *normal* mapping. The surrounding prose ("4h, not the usual 5h") was correct; only the worked example was inverted. Found while verifying expected values against `zoneinfo`/`tzdata==2026.3` before writing WP-003, rather than by a test failing later. Corrected values issued in WP-003's spec-correction notice; the docs themselves should be amended when next edited. Notable as a specification defect inside the specification written to prevent exactly this class of error — and as the justification for WP-003's standing instruction to report suspect expected values rather than code around them | Active |

| D-032 | **WP-003 (time engine) — APPROVED**, first submission, no correction needed | Verified independently rather than from the submitted tests: all DST conversion cases recomputed against `zoneinfo`/`tzdata==2026.3` (T1 corrected divergence, T3/T4 fold policies, T8 historical pre-2007 US rules, T12 pre-London window crossing the UTC date boundary in summer), plus an exhaustive consistency probe of 29,212 instants across all 7,302 trading days in 2007–2026 confirming `trading_day` and `trading_day_bounds` agree at every boundary including every DST transition. DST-day durations confirmed at 23h/25h. The T15 offset-literal lint rule was independently confirmed to catch an injected `timezone(timedelta(hours=5))` with a precise path:lineno message while correctly not flagging the adjacent legitimate `timezone.utc` | Active |
| D-032a | **Agent self-caught bug, recorded as a positive precedent**: the first `is_ambiguous` implementation returned True for non-existent times as well, because a skipped wall time also shows differing `fold=0`/`fold=1` offsets — routing spring-forward times into the ambiguous branch and returning a wrong instant. Found by a failing test rather than by inspection, fixed by additionally requiring the wall time to round-trip, and reported rather than quietly patched. DST gap resolution correctly uses binary search against the zone's own offset function rather than assuming a one-hour gap | Active |
| D-032b | **Second specification error by the PM, caught by the coding agent**: WP-003's T10 restatement claimed Friday 2023-06-09 17:00 NY "belongs to trading day 2023-06-12 (the following Monday)". The literal `trading_day()` formula in the same document (`local.date() + 1 day`, no weekend-skip) gives 2023-06-10, a Saturday. The original Technical Bible T10 entry makes no claim about which day the instant belongs to — the Monday claim was invented in the elaboration. Agent implemented the literal formula and flagged the contradiction, per the standing instruction. **Pattern now established across D-031 and D-032b: PM spec errors cluster in worked examples written from reasoning rather than computed. Mitigation adopted — every concrete expected value is computed against the real library before entering a work package** | Active |
| D-032c | **Minor debt: the `slow` pytest marker is unregistered**, so the 1,000,000-iteration property test runs on every default invocation (~19s of a ~28s suite) instead of being opt-in. Registering the marker requires `pyproject.toml`, which WP-003 forbade. Fold into the same future package as the `hypothesis` conversion (D-030a) | Deferred |

| D-033 | **Third PM specification error, caught by the coding agent during WP-004** | WP-004's `test_membership_returns_all_overlapping_sessions` stated that an instant at 03:00 London returns `{"pre_london"}` only. Wrong: 03:00 London is 12:00 Tokyo in GMT and 11:00 Tokyo in BST, both inside the `tokyo` session (09:00–15:00), so the correct result is `{"pre_london", "tokyo"}`. Agent noticed the conflict with observed behaviour, then verified via raw `zoneinfo` — deliberately bypassing its own implementation — before concluding the spec rather than the code was at fault. Correct order of suspicion; rules out a bug and a wrong expected value cancelling out | Active |
| D-033a | **Error pattern now confirmed across three instances (D-031, D-032b, D-033) and the mitigation sharpened** | All three PM spec errors are worked examples written from reasoning rather than computed. The mitigation adopted after D-032b — compute every concrete expected value — was *partially* applied in WP-004: session durations, the pre_london/london adjacency property, UTC boundaries and the 7,302-day containment sweep were all computed and all held. The membership example was not computed, because it appeared self-evident. **Refined rule: the examples that feel too obvious to verify are precisely the failing category. Compute every expected value without exception, including trivial-seeming ones — "obviousness" is not evidence and has now been wrong three times out of three.** | Active |

| D-035 | **WP-004 (session engine) — APPROVED** after one addendum | Verified independently, not from submitted tests: pre_london UTC boundaries (including the summer window starting on the previous UTC date), durations of 8h/7h/9h across London DST transitions, new_york ending exactly at the trading-day boundary, and the corrected `{pre_london, tokyo}` membership. The out-of-bounds anchoring guard confirmed to genuinely reject a `tokyo_early` session with a precise message naming the day and both boundaries. Cache invalidation confirmed in both directions by rewriting the same path valid -> invalid -> valid | Active |
| D-035a | **D-011 is now an executable, verified property rather than an argument** | `pre_london.end_ns == london.start_ns` holds for all 7,303 trading days in 2007-2026, zero violations. This is the justification for defining the Asian range in London local time rather than Tokyo local time: a Tokyo-anchored window's distance from the London open would drift four times a year for purely clerical reasons. The decision has been in the log since the technical specification; WP-004 makes it a test | Active |
| D-035b | **Fourth PM error, different in kind from D-031/D-032b/D-033** | The WP-004 addendum asserted that `load_session_set_def` had been decorated with `lru_cache`, creating a stale-result risk. It had not been — only `_find_transition_dates(tz, start_year, end_year)` was cached, keyed on facts about the IANA database rather than on any YAML file, carrying no collision risk. The claim came from the build narrative rather than from reading the committed code. Same root cause as the three expected-value errors: asserting without checking in a case that felt like it did not need checking. **Rule extended: claims about code state are verified by reading the repository, never inferred from a narrative summary — the same standard already applied to test results since D-028.** Agent correctly verified the actual state, said plainly the described bug did not exist, and then implemented the requested design anyway on its own merits | Active |
| D-035c | **The addendum was still warranted despite the wrong premise** | 8 of 9 validation tests share `session_set_id="test_set"`, `version=1`, and were safe only because `repo_root` is monkeypatched per test — safety by coincidence of test structure, not by design. A future validation test written without that monkeypatch would have silently collided. The content-aware cache key (path + mtime_ns + size) removes the dependency, and `test_cache_invalidates_when_file_changes` proves it. Suite runtime also fell from ~29s to ~10s | Active |

## 2026-08-30 — Stage 0 adversarial audit and resulting decisions

Audit document: `AUDIT_stage0_codebase_and_spec.md`. Twelve findings, all verified by execution against commit `57024e3`.

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-036 | **Feature warmup: ingest from 2006-09-01, count contexts from 2007-01-01** | Audit finding A-1, CRITICAL. Two Stage 0 contexts depend on `pre_london_range_pct(60)`, requiring 60 prior trading days. Ingesting from 2007-01-01 would leave the first ~60 trading days of 2007 unable to produce any context, deflating that year by ~23% for a purely mechanical reason. This lands specifically on the `P10 >= 60` gate leg, which is *designed* to be sensitive to the worst year — so a warmup artefact perfectly mimics the signal that leg exists to detect. At a true rate near 70/yr the deflated year reports ~54 and fails the gate alone, producing a false WIDEN CONTEXT caused by an ingest boundary rather than by GBP/USD. Measured: 2006-09-01 yields 86 warmup trading days (26 days of margin after typical holidays); 2006-10-01 yields only 65, too tight. Cost is 2,928 additional fetch hours, ~2% | Active |
| D-036a | **Vault read semantics: a query class's permitted READ range is `(-inf, period_end]`, not `[period_start, period_end]`. Evaluation range remains the period itself** | Consequence of D-036: warmup data precedes the development period, so a development run must be able to read 2006 while only generating opportunities and trades within 2007–2018. Data strictly older than a period cannot leak future information, so restricting the read range's lower bound protects nothing and would break warmup for every feature with a lookback. `periods.yaml` is unchanged; this is a `GatedReader` semantic to be implemented in Stage 1E. **Read range and evaluation range are separate concepts and must be separate parameters** | Active |
| D-037 | **Tick schema: drop `source_file_id`; amend Technical Bible §D.2** | Audit finding A-4, HIGH. The spec's normalised tick record includes `source_file_id` as an int32 FK; `parse.py` produces no such field. Resolution: remove it. Every tick in an hour comes from exactly one `.bi5` file, fully determined by `(instrument, hour)` which is itself determined by `ts_utc_ns` plus the Hive partition scheme. A registry, an id-assignment step and a join would recover information that is already implicit. Fetch-level provenance is covered by D-038 instead | Active |
| D-038 | **Fetch log sidecar: `data/raw/ticks/{instrument}/_fetch_log/{YYYY-MM}.json`** | Audit finding A-3, HIGH. `IngestReport.missing_hours` is computed, printed, then discarded. Consequently the filesystem cannot distinguish an hour the market was closed (0-byte blob, correct: no bars) from an hour never successfully fetched (no blob, incorrect: a silent hole). Both currently produce zero bars. The WP-001 live test produced 144 missing hours from 168, so this is not hypothetical. The dataset manifest (Stage 1B) is the eventual home, but the resampler (WP-005) and QA (WP-006) both precede it and both need this. The sidecar is fetch-level provenance, subordinate to and later consumable by the manifest — **not** a duplicate of it. Filesystem remains truth for blob existence; the log records *why* an hour has no blob | Active |
| D-039 | **Stage 0 intersection counting is per `(trading_day, direction)` tuple** | Audit finding A-9, MEDIUM. C-B and C-C count per direction; C-A and C-D count per day. The spec never defined whether `C-A ∩ C-B` on a day with both directions yields 1 or 2, a 2× ambiguity in exactly the quantity the gate thresholds are calibrated against. Resolution: count per `(trading_day, direction)`, with direction-less contexts broadcasting across both directions, because the eventual setup will be directional and the directional count is the one that matters. WP-008 must report both the directional and per-day counts so the choice remains visible | Active |
| D-040 | **Audit cadence: stage-boundary audits, not per-package** | Every one of A-1 through A-4 was invisible at package scope and appeared only when looking across the whole. Per-package review is structurally good at "does this package do what it claims" and structurally bad at "is the sequence right." Adopted: repeat this audit at the end of Stage 0 (before the probe runs) and at the end of Stage 2 (before any strategy machinery). The backtester execution semantics (Part M) get their own dedicated audit before Stage 4, as the highest-risk component in the project | Active |
| D-041 | **Remediation sequencing: WP-004b (core + sessions) → WP-004c (ingest + fetch log) → WP-005 (resampler)** | WP-004b precedes WP-004c because the fetch log should be serialised through the corrected `canonical_json` (A-6) rather than an uncorrected one. WP-004c precedes WP-005 because the resampler cannot correctly distinguish empty from unfetched hours without the fetch log | Active |

## 2026-08-31 — Stage 0F/0G completion and a storage-layer defect

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-043 | **WP-005 (resampler) — APPROVED**, then required a correction | Absent-minute semantics, bid/ask high independence, `spread_twa` arithmetic, `prev_gap_ns` across hour boundaries, hole detection and determinism all verified independently with hand-computed fixtures. Throughput 6.2 ms/hour vectorised vs 40.9 ms/hour via dataclasses (7x), reducing a 16-year pass from ~95 min to ~14.5 min | Active |
| D-044 | **WP-006 (QA suite) — APPROVED**, first submission | 16 checks implemented. Three Technical Bible §D.6 defects corrected in the package rather than inherited: the specified `run_checks(dataset_version_id)` signature was unimplementable (no `dataset_versions` until Stage 1B); `bid > ask` and `ask - bid <= 0` were the same check listed twice; and the DST check as written described a UTC anomaly that cannot exist. Agent added I-03/I-04 for "check skipped, insufficient samples", correctly distinguishing that from "check ran, found nothing" | Active |
| D-044a | **`thin_day_threshold` is now consumed** — the field deliberately deferred in WP-004 is read from the session set by W-05, closing that deferred item | Active |
| D-045 | **FIFTH PM ERROR — silent data loss approved into the storage layer** | `write_bars` replaced whole month files, so resampling any sub-range destroyed every other bar in that month. Verified: resampling 2024-01-15 then 2024-01-16 left only day 16, with no error and a well-formed frame containing half the data. **The coding agent flagged this as a theoretical risk in WP-005's own closing notes and I approved WP-005 without acting on it.** My determinism test resampled the *same* range twice — the one case that structurally cannot detect it. Fixed by merge semantics in `write_bars` plus an unconditional `prev_gap_ns` recompute over the sorted merged frame | Active |
| D-045a | **The error pattern has changed and the mitigation must change with it** | D-031, D-032b and D-033 were wrong expected values written from reasoning; D-035b was a code-state claim inferred from a narrative. All four were fixed by "compute/read rather than assert." **D-045 is different: the fact was correctly stated, in writing, by the agent — and I read it and did not act.** The failure was not verification but triage: a risk noted in closing notes was treated as informational rather than as a finding requiring a decision. **Rule adopted: every item a coding agent raises in closing notes gets an explicit disposition in the review — acted on, deferred with a decision-log entry, or dismissed with a reason. Silence is no longer an acceptable response to a flagged risk.** | Active |
| D-045b | **Unconditional `prev_gap_ns` recompute preferred over recompute-on-merge** | Agent's improvement on the specified fix. Running the recompute on every write, not only on merges, means exactly one code path produces the column for stored data, and makes `prev_gap_ns` a pure function of the stored frame rather than of run history. Two paths that "should" agree is how D-045 got in | Active |
| D-045c | **`ResampleReport.bars_written` means "bars this call contributed", not "bars now in the file"** | Flagged by the agent, deliberately left unchanged. Making it the post-merge total would require reading back what was just written. Recorded so no future caller assumes the other meaning | Active |

## 2026-08-31 — Stage 0 complete

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-046 | **WP-007 (feature engine) — APPROVED** after two follow-ups | Five features plus an auto-generated leakage harness. Verified independently by injecting three distinct leak shapes — a one-bar peek, a whole-frame mean normalisation, and a reverse cumulative sum — all caught. The third is the case L-1 alone misses and is why L-2 exists. Session-terminal nulling verified across 4,135 bars (zero non-null before 08:00 London), mid-price usage confirmed against bid- and ask-only ranges, `atr_bars` shown to count present bars (identical ATR for contiguous vs 3-hour-gapped frames of equal bar count), `rv_60m` gap-nulling clean across 4,076 windows | Active |
| D-046a | **PM spec error in WP-007's L-2: row shuffling would have broken the ascending-`ts_utc_ns` invariant**, failing correct features for a reason unrelated to leakage. Corrected mid-flight to perturb price columns only. Notable because the natural response to a failing leakage test is to suspect the feature — the wrong place to look, and the path toward weakening a test to make code pass | Active |
| D-046b | **Test collection was invocation-dependent** — the leakage harness imported fixtures via `from tests.unit.test_features_library import ...`, which resolves only under `python -m pytest` (which inserts CWD into `sys.path`) and not under bare `pytest`. A *collection* error, which some CI configurations report as "0 tests" rather than a failure, meaning the single most important guard in the package would have silently not run. Fixed by extracting shared builders into a plain `_feature_fixtures.py` module. **Standing rule: a test module is not a library; never import fixtures from one test module into another.** Verification now runs both invocation forms | Active |
| D-047 | **WP-008 (Stage 0 probe and gate) — APPROVED** after one correction | Four contexts, per-`(trading_day, direction)` intersection counting, the two-tier gate, and a report carrying the watermark, the D-024a qualification and the C-C forward-window caveat. Classified STANDARD rather than premium: the hard correctness work (DST, sessions, leakage, absent-minute semantics) was already done and verified; this composes verified pieces and applies arithmetic | Active |
| D-047a | **SIXTH PM ERROR — vault boundary specified as `>=` against a half-open end** | WP-008's acceptance criterion 7 required refusing `--to 2023-01-01`. Implemented literally, that made the last readable instant 2022-12-30T23:59:59.999Z, **silently excluding 2022-12-31 from the gate** — on the P10 leg deliberately most sensitive to the worst year. Written without checking against the half-open `[start, end)` convention used since WP-001. Fixed to strict `>`, so the boundary itself is accepted and reads through 2022-12-31T23:59:59.999Z, touching no vault data. **Found and flagged by the coding agent, which implemented the criterion literally and then reported the consequence — exactly the behaviour D-045 was adopted to elicit** | Active |
| D-047b | **Known gap, deliberately not fixed: thin days are excluded from QA reporting but NOT from probe counting** | Technical Bible §G.0.4 says thin days are excluded; no `thin_day` flag exists for `probe` to consume, since QA's W-05 computes an analogous notion internally as a reporting finding only. Building a second detector inside `probe` risks drifting from QA's definition. **If a probe run shows an implausible count on a holiday-heavy year, this is the first thing to check** | Deferred |
| D-047c | **`python -m jarvis.cli.main` silently does nothing** — no `__main__` guard, so the module imports and exits with no output and exit 0. Found during review when a subprocess harness produced four false negatives. Harmless today but a latent trap: anyone reaching for `-m` gets silence rather than an error. Use the `jarvis` console script | Deferred |
| D-048 | **STAGE 0 IMPLEMENTATION COMPLETE** — nine milestones (0A–0I) plus three audit-remediation packages, 327 tests passing under both pytest invocations, 11/11 architecture contracts. Six PM specification errors and three coding defects found and fixed; every one caught before reaching a research result | Active |


## 2026-09-01 — Data source change: Dukascopy to HistData

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-049 | **Dukascopy `.bi5` hourly endpoint abandoned as the bulk acquisition route** | Measured from a clean home connection: 8–20s TTFB, ~12.5% request failure, 3/5 outright connect failures in a bare curl loop, while a Cloudflare control pulled 1 MB at 3 MB/s from the same machine. Concurrency testing (8/16/32) produced non-monotonic throughput with failures rising 3→5→6; best extrapolation was 6–12 days with ~18,000 holes. Tickstory's support forum documents the same errors from other users since late 2025, with the explanation that Dukascopy registers errors on bulk download. This is their steady state, not an outage | Active |
| D-049a | **The Dukascopy fetcher (WP-001/WP-004c) is retained, not deleted** — it remains the incremental path for forward-testing (Stage 6), where a day at a time with resumability is entirely survivable. Only the 16-year backfill moves | Active |
| D-050 | **PM error: HistData was wrongly disqualified in Doc 1 §C3** as "free, 1-min, mid only, no spread". True of their M1 bars, which are bid-based — **false of their Generic ASCII tick format, which carries both bid and ask**. This error is why three sessions were spent fighting Dukascopy. Corrected on reading HistData's own FAQ | Active |
| D-051 | **Research data source changed from Dukascopy to HistData.com** (amends D-003 / F-06) | Format `YYYYMMDD HHMMSSmmm,bid,ask,volume`. Verified on GBP/USD May 2017: 1,331,192 ticks, **zero rows with bid ≥ ask**, spread min 0.10 / mean 0.94 / max 8.00 pips. Density median 36 ticks/min, p10 = 11, only 2.9% of minutes below 5 ticks — intrabar ambiguity resolves from ticks in ~97% of cases. GBP/USD available from May 2000, six years of margin on the 2006-09 start. ~7 MB zipped per month, ~1.4 GB for the full 196-month range versus 6–8 GB of `.bi5`. No account, no rate limit, no throttling | Active |
| D-051a | **Volume is always 0 in HistData files** — documented by them; FX volumes are broker-specific so they strip it. The tick schema already has `bid_volume`/`ask_volume` nullable, so this costs nothing | Active |
| D-052 | **CRITICAL — HistData timestamps are `America/New_York` local WITH DST, not the fixed EST their FAQ claims** | Their FAQ states "EST time-zone WITHOUT Day Light Savings adjustments". The data contradicts it. Confirmed by three independent anchors: (1) NFP, released 08:30 NY local, spikes at stamp 08:30 in both EST and EDT months; (2) the weekend close lands at 16:59 in EDT months, where a fixed −5 stamp would give 15:59; (3) the 16:00 London WMR fix moves to stamp 12:00 during the March US/EU DST divergence window and back to 11:00 either side — only possible under NY-local stamps, and it also confirms they follow **US** transition dates, not European ones. **Had the FAQ been trusted, every timestamp from March to November would be an hour off — roughly two-thirds of every year — shifting every session boundary and appearing as a market regime rather than a bug** | Active — correct for Era A (2006-09–2018-12); see D-055 for the full-range model |
| D-052a | **HistData use correct historical US DST rules, not modern rules projected backward** | November 2004 falls under the pre-2007 regime (DST ended last Sunday of October). The 1–5 November window is discriminating: correct historical rules predict the London fix at stamp 11:00, modern rules projected backward predict 12:00. Observed concentration 168 vs 93 in favour of correct historical rules. This is the same failure class WP-003's T8 test guards against in our own code, verified here in the data | Active |
| D-052b | **HistData CHANGED convention between October 2022 and November 2023** | 29 EDT Fridays across 2014, 2017, 2018, 2019, 2020, 2021, 2022-05 and 2022-10 all read NY-local (close at 16:59). 2023-11-03 reads fixed UTC−5 (close at 15:59), corroborated by NFP on the same day spiking at 07:30 rather than 08:30. It appears HistData *corrected* their data to match their FAQ at some point in that window. **The ingest range 2006-09 → 2022-12 is entirely NY-local and unaffected** — October 2022 is the last EDT month in range and confirms it. Anyone extending the dataset past 2022 must handle both conventions | **Superseded in part by D-055** — the "entirely NY-local and unaffected" claim is false (see D-055); the "second change by Nov 2023" claim is now believed unnecessary, since Nov 2023 falls inside Era B's ordinary autumn divergence window and would read exactly as observed without any further vendor change. Unconfirmed — 2023+ data was outside this investigation |
| D-052c | **WP-009 will detect the timezone convention per file, not assume a constant** | For each monthly file: locate every Friday weekend close falling in an EDT period; 16:59 means NY-local, 15:59 means fixed UTC−5. HALT if a file is internally mixed, or if a file has no discriminating Friday and its neighbours disagree. Strictly better than a hardcoded constant — a future convention change fails loudly instead of silently shifting sessions, and the dataset can be extended past 2022 without re-litigating this | **Superseded by D-055a** — a file being internally mixed is not necessarily corruption; a Era B March/October file legitimately holds two offsets |
| D-052d | **Test that can never work, recorded so it is not attempted again**: checking for zero ticks in the spring-forward hour or doubled ticks in the fall-back hour. US DST transitions always occur Sunday 02:00 local, which always falls inside the FX weekend closure (Fri 17:00 → Sun 17:00 NY). No FX dataset will ever have ticks in a transition hour | Active |
| D-053 | **Data quality note: May 2023 is an outlier month** with 135 intraday gaps exceeding one hour (worst 120 min), against zero such gaps in 2017-05, 2022-05, 2022-10 and 2023-11. A single bad month, not systematic degradation. Outside the ingest range; relevant only if the dataset is extended | Active |
| D-053a | **October 2022 shows 6.3M ticks (280k/day) against a ~55–85k/day norm** — roughly 5× density. This is genuine: the UK mini-budget gilt crisis, when GBP hit record lows. Not a data artefact. Worth remembering when it appears as an outlier in Stage 2 descriptive statistics | Active |
| D-054 | **Investigation credit**: the convention change (D-052b) was found only because the user pushed for additional years after four files from 2017 looked conclusive. Four files from a single year would have produced a confident, wrong answer. **Rule adopted: a data-convention claim requires discriminating evidence spanning the full ingest range, not a single period** | Active |

## 2026-09-12 — HistData timezone: corrected model (full-archive investigation)

Investigation ran against the complete 196-month archive (2006-09–2022-12), not sample months. Follows directly from D-054's rule — the sample-month investigation (D-052/D-052b/D-052c) produced a confident but incomplete model; the full-range investigation replaces it.

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-055 | **CORRECTED MODEL — no HistData file is on a fixed UTC−5 clock; two DST calendars, one switch, mid-2019** | Era A (2006-09–2018-12): `America/New_York` wall clock, changing on US dates (2nd Sun Mar / 1st Sun Nov). Era B (2019-01–2022-12): UTC−5/−4, changing on EU dates (last Sun Mar / last Sun Oct), with the switch instant itself landing at 00:00 UTC on the **Monday following** the EU transition (~23h after the 01:00 UTC instant) — not at the transition itself. The two calendars agree ~11 months a year and diverge only inside two short annual windows (spring: US-change→EU-change; autumn: EU-change→US-change), which is exactly why the sample-month investigation saw "some Marches and Novembers" behaving oddly and mistook it for file-level corruption. Verified by three independent anchors, full archive, zero exceptions: weekend boundaries on divergent weeks (94/94 — 70/70 us_dst 2007–2018, 24/24 eu_dst 2019–2022); the stamp clock's own discontinuities (exactly 8, all on EU change dates 2019–2022, matching the predicted boundary to within seconds, none in 2006–2018); the 16:00 London WMR fix (lands correctly under the rule in both eras, displaced exactly one hour under the wrong rule in both). Whole-archive parse: 33 files us_dst (all 2006-10–2018-11), 11 eu_dst (all 2019-03–2022-11), 151 needing no determination, 1 failed for an unrelated reason (D-055g/D-055h). Zero files cross the era boundary. **Supersedes D-052's and D-052b's range-wide claims** — see D-052/D-052b status | Active |
| D-055a | **Supersedes D-052c's per-file HALT-on-mixed-convention rule** | An internally inconsistent file is not necessarily corrupt. An Era B March or October file legitimately holds two offsets within itself — this is the file correctly crossing its one annual divergence window, not damage. `parse_histdata_csv` now assigns the UTC offset per row at the verified switch point rather than one convention per file | Active |
| D-055b | **October 2019–2022 files are non-monotonic by design, not corrupt** | The EU fall-back re-uses an hour already stamped earlier in the same file. The previous strict-ascending check rejected all four October files outright. Exactly one backward step, only at the file position the rule predicts, is now permitted; a second backward step is still treated as corruption. The repeated hour is resolved by file position, not wall-clock fold — both passes carry identical wall-clock stamps and only file order distinguishes them | Active |
| D-055c | **The per-file convention hint (D-052c) is removed, not just superseded** | The carried-hint mechanism existed to bridge months with no internal discriminating evidence by inheriting a neighbour's convention. That is exactly the mechanism that would silently carry a convention across an undetected changeover. A month now either proves its own clock from its own content, or needs no determination at all — no month's classification depends on a neighbour's | Active |
| D-055d | **The "quarantine" bucket anticipated after the sample-month investigation is empty under the corrected model** | A file can only be blind (no internal discriminating evidence) where the two calendars already agree for every date it contains — and where they agree, both produce the identical UTC instant for every tick. December, 2020-11, and the general EST season were never actually at risk; they are unreadable only because nothing depends on reading them. The two questions left open after the sample-month pass were both detector artefacts, not properties of the data: (1) 2022-11 — the daily-rollover anchor cannot read a Friday or Sunday at all, since the rollover quiet period coincides with the weekend closure; on the weekend-boundary anchor all four pre-transition days read and agree on `eu_dst`. (2) 2019-03/2022-03 — not disagreement; 2022-03 genuinely changes clock partway through the month at a verified weekend boundary, and 2019-03's divergence window runs to the end of the month with no post-switch days to disagree with. A per-file label was being asked an unanswerable question in both cases | Active |
| D-055e | **The previously reported "April bleed" (2021-04-01/02, 2022-04-01/02) does not exist in the data** | April contains no divergent dates in any year — the EU change is always in March — so there is nothing in an April file for a convention to be wrong about; both calendars agree on every tick. Checked directly: every April Friday close in 2019–2022 lands at 16:59 New York, exactly as both calendars predict. The original finding traces to the WMR-fix anchor built during the sample-month investigation, which was a reconstruction from documented values rather than a literal reuse of prior validated code, and had only ever been audited for transition-date arithmetic (D-052/D-052a-style correctness), never for ordinary non-transition days. Recorded as a general lesson: a diagnostic tool passing the specific audit it was given is not the same as the tool being correct everywhere it runs | Active |
| D-055f | **The 2018→2019 changeover date cannot be narrowed further, and this is permanent and harmless** | Last Era A evidence: the 2018 autumn window (reads us_dst). First Era B evidence: the 2019 spring window (reads eu_dst). Between them the two calendars agree, so nothing in the data can locate the exact changeover date — but every file in that gap converts identically under either assumption, so the gap carries zero risk to any downstream computation | Active |
| D-055g | **Fixed: the spread sanity check rejected genuine zero-spread quotes, making 2009-11 unimportable** | The check rejected `bid >= ask`; the real file has exactly five rows with `bid == ask` (2009-11-13, ~15:30) and zero rows with `bid > ask` — exactly the zero-spread quotes `detect_column_order`'s own code comment already cited when setting its 1% tolerance. Changed to reject only `bid > ask`; a true inversion is still treated as corruption | Active |
| D-055h | **Known gap, not fixed, needs a decision: 2009-05 changes column order mid-month at a clean weekend boundary** | `detect_column_order` correctly refuses the file (76% ask-bid overall — neither ≥99% nor ≤1%), but the file is not ambiguous: May 1–22 is 100% ask-bid, May 24–31 is 0% (i.e. bid-ask), switching cleanly at a weekend boundary — structurally the same lesson as D-055/D-055a, a different convention. Corrects the existing code comment calling 2009-05 "a clean transition month" — it is a transition month, but the transition is inside it. Left unimplemented pending a decision: extend `detect_column_order` to operate per-day under the same ≥99%/≤1% rule and require any switch to fall on a weekend boundary (mirrors the per-row timezone fix in D-055a), or accept 2009-05 as a permanently excluded month (1 of 144 Development/Validation months) and log it as a known gap | **Resolved by D-056** |

## 2026-09-12 — WP-009 complete: HistData ingest, full 196-month range

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-056 | **WP-009 IMPLEMENTATION COMPLETE — HistData ingest, full 2006-09..2022-12 range, all 196 months on disk** | Built on the `wp-009-tz-finding` branch (D-055 series). This package closed the one remaining open item (D-055h) and ran the corrected two-era, per-row model for real: `detect_column_order` now falls back to a per-DAY check, under the same ≥99%/≤1% rule, ONLY when the whole-file check is inconsistent, and accepts a single switch only when it lands exactly on a weekend boundary — otherwise it still raises exactly as before, naming the day and fraction, never guessing. 2009-05 (May 1–22 ask-bid, May 24–31 bid-ask, switching at the May 22/24 weekend) now imports correctly under this fallback. Also fixed en route: the `data import-histdata` CLI command referenced `report.conventions`, a field renamed to `stamp_clocks` earlier on this branch — it would have raised `AttributeError` on its first real invocation, since no test exercised that path. Corrected, and its "MORE THAN ONE convention" warning removed, since a mix of `us_dst` and `eu_dst` months in one import is the *expected, correct* outcome for any range spanning 2018/2019 (D-055), not a fault. **Full real import run** (not a validation pass — actual Parquet written to `data/tick/instrument=GBPUSD/`): 196/196 months present on disk, zero failures. Per-era tally: 33 `us_dst` (all 2006-10..2018-11), 11 `eu_dst` (all 2019-03..2022-11), 152 `not_required` (both clocks agree on every stamp in the file, including the now-fixed 2009-05) — exactly 33+11+152=196, matching D-055's read-only investigation with 2009-05 moved from "failed" to "not_required". Total ticks on disk: 255,587,623 (see D-057 for why this is lower than the 259,259,448 rows actually parsed from source — a pre-existing, out-of-scope characteristic of `write_ticks`, not of this package). Acceptance check repeated against the real on-disk store, not just the in-memory parse D-055 validated: every Friday weekend close across all 196 months lands at 17:00 New York, with exactly the same two documented holiday exceptions D-055's investigation already found (2010-12-24 Christmas Eve early close, 2020-12-25 Christmas Day) — zero timezone-attributable exceptions. 375 tests pass under both `pytest` and `python -m pytest`; 11/11 architecture contracts held. The dataset is ready for Stage 1A's resample step | Active |
| D-057 | **Flagged, not fixed: a pre-existing, out-of-scope dedup collapse in 2007–2011 raw files, found while verifying the WP-009 import's tick totals** | The real import's on-disk tick total (255,587,623) is 3,671,825 rows lower than the sum of `row_count` actually parsed from source across all 196 months (259,259,448) — a ~1.4% gap, concentrated entirely in 2007–2011 (up to 32.76% of one month's rows, 2010-05) and entirely ABSENT from every 2019–2022 (`eu_dst`) file (measured 0.00% in 2022-03). Directly inspected: the raw 2007–2011 files genuinely contain multiple rows sharing an identical millisecond timestamp with a DIFFERENT quote — a real characteristic of the source feed's export resolution in that era, confirmed to have nothing to do with this package's per-row offset or per-day column-order logic (the drop rate is zero for every `eu_dst` month and roughly proportional to raw row count elsewhere, not concentrated on any DST-switch date). `write_ticks`'s existing dedup (`unique(subset=["ts_utc_ns"], keep="last")`, established under D-045, unmodified by this package) collapses each such cluster to the last row sharing that timestamp, discarding the earlier one(s) silently. Left unfixed and reported here rather than silently absorbed into WP-009's tick-count figure, per D-045a: this is genuinely outside this package's remit (stamp clock and column order, not the tick store's dedup key), and fixing it would mean a product decision — keep last-quote-wins (current, silent), key dedup on the full row instead of `ts_utc_ns` alone, or add a synthetic sub-millisecond tie-breaker — none of which this package is scoped to make | **Resolved by D-058** |

## 2026-09-12 — WP-009g: the tick-collision dedup gap (D-057) is fixed

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-058 | **`write_ticks`'s dedup key extended to `(ts_utc_ns, row_sequence)`; genuine same-millisecond quotes no longer silently discarded** | **Lineage, confirmed before writing this entry (per this package's own instruction):** `write_ticks` (`jarvis/ingest/histdata_import.py`) and `write_bars` (`jarvis/bars/store.py`) are DISTINCT functions in DISTINCT modules storing structurally different things. D-045 fixed `write_bars` replacing whole month files instead of merging; it never reviewed, and says nothing about, either function's DEDUP KEY choice. `write_ticks` was written by copying `write_bars`' merge-then-dedup-then-sort SHAPE, inheriting a dedup-on-`ts_utc_ns` key that is correct for bars (one bar per minute, by construction) but was never independently checked for ticks (no such 1:1 guarantee) — until WP-009f found the gap it was silently causing. This entry's lineage is its own, not inherited from D-045 by association. **Fix:** `write_ticks` now dedupes on `(ts_utc_ns, row_sequence)`, where `row_sequence` is the tick's real, honest 0-indexed position in its source file for the month (`TICK_SCHEMA` gains this column) — never a fabricated timestamp, never implying precision finer than `ts_utc_ns`'s real millisecond resolution. This mirrors an existing precedent already in this codebase: `jarvis.ingest.parse.Tick.seq` ("ordinal within the source hour — the tie-breaker") serves the identical purpose for the Dukascopy path, just never needed to be a stored column there since `TickArrays` never persists to Parquet. A TRUE full duplicate (identical `ts_utc_ns` AND bid AND ask — a literally repeated row) still collapses to one row exactly as before; only genuinely differing same-millisecond rows are now preserved. Re-import-must-win semantics are preserved: a re-import's row still wins at any shared `(ts_utc_ns, row_sequence)` identity. Full archive rebuilt from scratch (the pre-fix on-disk store was deleted rather than migrated — it predates `row_sequence` entirely, and merging fresh data onto data already built under the lossy key would risk masking what it lost; `write_ticks` now refuses that merge outright with a named `IntegrityError` rather than allowing it silently). **Result:** 196/196 months re-imported; 259,259,448 rows parsed, 259,014,364 on disk, difference 245,084 — all confirmed genuine full duplicates (a materially more precise count than WP-009f's coarser per-group estimate of 50,739: WP-009f classified each same-millisecond group as wholly "harmless" or wholly "collision," which cannot see a group that is BOTH — e.g. 2010-04 alone has 28,523 such mixed groups, contributing 33,423 rows that are true duplicates of another row in an otherwise genuinely-colliding group; `write_ticks`'s own key finds these precisely because it is the real mechanism, not a diagnostic approximation of it). **2010-04 spot check** (the 38.0% peak month WP-009f found): 427,887 rows on disk before this fix, 656,812 after — 228,925 previously-discarded distinct quotes recovered, net of that month's own 33,423 true duplicates. Friday-close-at-17:00-New-York sanity check (D-055/WP-009's own) re-run against the corrected dataset: 196/196 months checked, only the same two pre-existing holiday exceptions (2010-12-24, 2020-12-25) — confirms the fix touched only the dedup key, not any `ts_utc_ns` value. **Checked, per this package's own requirement: does anything else assume `ts_utc_ns` uniqueness among ticks?** No code anywhere reads `data/tick/` except `write_ticks`'s own merge-read-back (now fixed) — confirmed by exhaustive search; `bars/resample.py` reads Dukascopy `.bi5` blobs directly (the pre-existing, separately-flagged wiring gap), and `qa/checks.py`'s tick-level checks operate on Dukascopy `TickArrays`, never on this store. `bars/store.py` and `features/compute.py` each key their OWN uniqueness-on-`ts_utc_ns` assumption against their OWN separate stores (bars, features), where it remains genuinely valid (one bar/feature row per minute, by construction) — unaffected by this change. 380 tests pass under both `pytest` and `python -m pytest`; 11/11 architecture contracts held. The dataset is genuinely ready for Stage 1A's resample step, not provisionally so | Active |
| D-059 | **D-049a superseded — Dukascopy is no longer retained for forward-testing** | HistData updates its current in-progress month on a roughly weekly cadence for major pairs including GBP/USD (confirmed via their own site's update history and their weekly auto-update service, which explicitly covers GBP/USD). This closes the gap D-049a was written to bridge — a ~1-week lag is immaterial against this project's ~1hr/day, patient, session-based research cadence, and no part of the design requires same-day data. There is also a methodological argument beyond convenience: sourcing backtest data from HistData and forward-test data from Dukascopy would mean Stage 6 results come from a different vendor's tick composite than everything upstream of it — different liquidity sources, unverified spread characteristics, none of it subjected to the scrutiny HistData's ingest just went through (D-052–D-058). Given C7/C7b's emphasis on realistic, consistent execution modelling, using one verified source throughout is the more defensible design, not just the simpler one. **The Dukascopy fetcher and its resample-path tests are left in the repository, dormant** — not deleted, since working tested code costs nothing sitting unused and deletion forecloses a need nobody has actually hit — but preserving compatibility with it is no longer a constraint on any future package's design | Active — supersedes D-049a's forward-testing rationale; D-049 (Dukascopy abandoned for bulk backfill) is unaffected |
| D-060 | **WP-010: `resample_range` and `run_checks` retargeted in place to read `data/tick/`; the Dukascopy-blob wiring gap flagged in D-058 is closed** | **Design choice, stated plainly:** retargeted `bars/resample.py::resample_range` and `qa/report.py::run_checks` IN PLACE, rather than adding a parallel function — D-059 removed the only reason a parallel path would have been needed (preserving Dukascopy as a live, load-bearing forward-testing source), so the simplest design is now also the correct one. `jarvis/ingest/` (histdata.py, histdata_import.py) is untouched, as required. **What changed:** both functions now iterate MONTHS (`data/tick/instrument=.../year=.../month=.../data.parquet`, via the existing `tick_path`) instead of Dukascopy HOURS (`.bi5` blobs) — HistData's own storage granularity, and small enough (worst case ~1.3M rows/month, per WP-009's real import) to hold entirely in memory per month, same discipline as before just at the grain the source actually has. A HOLE is now a wholly-missing MONTH file, not a missing hour; a present month contributing zero ticks to part of the requested range is explicitly NOT a hole (WP-005's "0-byte blob = market closed" classification has no equivalent need once the source is a monthly, not hourly, unit). `ResampleReport` and `QAReport`'s fields were renamed to match (`hours_*` → `months_*`/`ticks_read`; `hours_examined` → `months_examined`) — a real, disclosed API change, not a silent one. **row_sequence handling (D-058), the one genuinely new behaviour this package had to verify, not just relocate:** confirmed directly by a new test (`test_open_and_close_use_row_sequence_order_not_numeric_sort`) that two ticks sharing an identical `ts_utc_ns` are both retained (no silent drop) and that a bar's open/close correctly reflect `row_sequence` (file) order, not a numeric sort of price — the same tie-break principle the Dukascopy path already relied on via `Tick.seq`, now exercised against a real compound key for the first time. **QA's Dukascopy-specific fetch-log checks (E-04/W-06 missing hours, E-05 malformed blob, E-06 fetch log/filesystem disagreement) have no equivalent for a monthly-import store** — a month either has a Parquet file or it doesn't, and `resample_range` already raises on that; `run_checks` no longer calls `FetchLogChecksAccumulator`. The class itself is left in `qa/checks.py`, dormant, still directly unit-tested — not deleted, matching D-059's own "working code costs nothing sitting unused" reasoning. One end-to-end test (`test_run_checks_malformed_blob_surfaces_as_e05_not_a_crash`) was removed rather than adapted, since the capability it exercised (E-05 surfacing through `run_checks`) no longer exists on the retargeted path; E-05 itself remains covered directly against `FetchLogChecksAccumulator`. **`_check_weekend`'s per-hour cheap pre-filter was rewritten as a fully vectorised per-batch one** (Polars `dt.weekday()`/`dt.hour()` over the whole month's tick array) rather than dropped, since removing it would mean paying `is_weekend_gap`'s per-tick zoneinfo cost across an entire month unfiltered — same over-include-never-under-include principle, adapted to the new batch size. **Disclosed scope deviation:** `src/jarvis/cli/main.py` was also touched (not in this package's original file-allow-list) — a strictly necessary consequence of `ResampleReport`'s field renames, since the `data resample` command's print statements would otherwise raise `AttributeError` on first real invocation; not additional scope, flagged per this project's standing convention rather than left silent. **Verification:** a new end-to-end regression test resamples a synthetic-but-realistic month shaped like a real divergent-era month (2020-03) directly from `data/tick/`, confirming non-empty, structurally sane bars. Every existing resample/QA test was ported to write into `data/tick/` (via the real `write_ticks`) instead of raw `.bi5` blobs, with a handful reframed where the underlying concept changed (hour-level hole → month-level hole; "market closed hour" → "present month, quiet sub-range") — same intent, honest about the new model rather than forcing old semantics onto it. 380 tests pass under both `pytest` and `python -m pytest`; 11/11 architecture contracts held (dependency count actually dropped, 116→111, from removing the Dukascopy-specific imports this path no longer needs). Stage 1A's Resample→Validate→Features→Probe chain is unblocked against the corrected HistData dataset for the first time | Active |
| D-061 | **WP-012: `probe.report.year_admissibility` retargeted to `data/tick/`; vault-boundary enforcement extended to `data resample`/`data validate`/`features build`** | **Bug found by WP-011's runbook-writing process, not by this package:** `year_admissibility` (untouched by WP-009/WP-010 — `probe/` was out of scope for both) still measured admissibility via `jarvis.ingest.urls.raw_blob_path`, a Dukascopy raw-blob path holding 224 leftover hours from an unrelated 2024-01 diagnostic and zero real 2006–2022 coverage. Every year measured a 0.0 hours-present ratio and failed admissibility before `run_checks` was ever reached; with `_MIN_ADMISSIBLE_YEARS = 12` and zero years ever passing, `evaluate_gate` returned `INSUFFICIENT_DATA` unconditionally, regardless of how complete the real, corrected `data/tick/` store actually was — see `docs/STAGE_1A_RUNBOOK.md` Section 0. **Fix:** `_hours_present_ratio` replaced by `_months_present_ratio`, reusing WP-010's own "a hole is a wholly-missing month" model (`tick_path(...).is_file()` per calendar month) rather than inventing a new signal, applied at the same 0.95 threshold against 12 months instead of trading-week hours. A partial year (2006: only 2006-09 through 2006-12 on disk, D-036's warmup range) correctly fails on months alone (4/12 ≈ 33%) — this is the actual mechanism D-036a relies on to exclude warmup-only years from the gate without a separate truncated read range, now working as intended rather than accidentally working for the wrong reason. **Full `probe/` audit, as required, not just a targeted patch:** every function in `contexts.py`, `gate.py`, `__init__.py`, and the remainder of `report.py` read in full. `_hours_present_ratio`/`year_admissibility` was the ONLY stale Dukascopy-era reference found — `contexts.py` and `gate.py` operate purely on already-loaded `bars`/`features`/`YearCounts` structures with no filesystem access at all; `report.py`'s hour-alignment validation (`NS_PER_HOUR`) is a general input-shape check, not a Dukascopy assumption, and is now sourced from `jarvis.timeengine` instead of `jarvis.ingest.urls` (which `report.py` no longer imports from at all, having lost its last reason to). **Vault-boundary gap, closed:** confirmed exactly which commands lacked a code-level vault-boundary check — `data resample`, `data validate`, and `features build` had none at all; only `stage0 probe` did. Extracted the check into `probe.report.reject_vault_range(end_ns, *, caller)`, called by all four commands (`run_probe` now calls it too, replacing its own inline copy — one implementation, not two that could drift) so the boundary can no longer depend on a future command remembering to add it. `data fetch` (Dukascopy, dormant per D-059, not part of the live pipeline) and `data import-histdata` (YYYY-MM-scoped, governs what gets imported into `data/tick/`, a different question) were checked and deliberately excluded — WP-012 scoped this to the four Stage 1A pipeline commands specifically, per its own framing. **Disclosed limitation, not claimed away, tracked in its own entry (D-061a) rather than buried in this one's prose:** this enforcement lives at the CLI entry point only — see D-061a for exactly what that does and does not cover. **Disclosed scope deviation:** two pre-existing tests in `tests/unit/test_qa_report.py` (`test_cli_exit_code_zero_when_no_errors`, `test_cli_exit_code_three_when_error_present`) used a 2024 date literal for `jarvis data validate` purely as an arbitrary recent test date; `data validate`'s new vault-boundary check now correctly refuses it. Updated both to 2020 — a date-literal fix only, zero change to QA's own behaviour or coverage, disclosed rather than left silent per this project's standing convention. **Verification:** new tests confirm `year_admissibility` reads `data/tick/` (never creating a `data/raw/` directory at all), correctly fails a partial year, and still enforces the QA-errors half; a direct regression test builds 20 years of realistic synthetic `data/tick/` coverage and confirms `evaluate_gate` no longer returns `INSUFFICIENT_DATA` unconditionally (it would have, against this exact test, before this fix); each of the three newly-guarded commands gets its own accept-the-boundary/refuse-past-it test pair, mirroring `stage0 probe`'s existing one. 390 tests pass under both `pytest` and `python -m pytest`; 11/11 architecture contracts held. `docs/STAGE_1A_RUNBOOK.md` Section 0's blocker is resolved, confirmed by re-reading it against this fix, not assumed closed because this package ran — the runbook document itself has now been updated to match (see its own revision note) | Active |
| D-061a | **Known gap, deliberately not fixed: vault-boundary enforcement is CLI-level only, not a property of `resample_range`/`run_checks`/`compute` themselves** | `bars/resample.py`, `qa/report.py`, and `features/compute.py` were explicitly out of WP-012's file scope, so `reject_vault_range` could be wired into `cli/main.py`'s four command functions but not into the underlying library functions those commands call. `run_probe` is the one exception, since `probe/report.py` was in scope — it enforces internally regardless of caller, unchanged from before. A programmatic call to `resample_range`, `run_checks`, or `compute` that bypasses the `jarvis` CLI entirely is NOT protected by this fix. The vault boundary is therefore enforced for every command a human actually runs, but is not yet the type-level property AR-5 asks for — closing that fully means touching the three modules this package was forbidden from touching | Deferred |
| D-062 | **Stage 1A QA-finding-severity handling rule, adopted and exercised** | `docs/STAGE_1A_RUNBOOK.md` Section 4 proposed, and this project's first real operational run of `jarvis data validate` against the corrected dataset exercised: any ERROR finding, on any month, halts the run pending review before Features/Probe are allowed to proceed — WARNING/INFO findings are compiled into the run's own summary but do not block. Rationale unchanged from the runbook's own text: an ERROR at this stage plausibly indicates a real problem worth seeing before it feeds a research result everything downstream will trust; WARNING/INFO are not evidence of that. **Exercised for real, not hypothetically:** the actual Stage 1A run (2026-09-13) halted correctly at Step 2 on a single ERROR finding (E-01, non-positive spread, count 11) — Steps 3/4 were not run, no parameter was adjusted to get past it, and the finding was instead reported and handed to WP-013 as its own package, exactly as this rule specifies. This entry exists because WP-011 explicitly required the rule get its own decision-log entry once adopted rather than being assumed into force by a runbook document alone; it had governed one real run before this entry was written, which WP-013 closes as process debt | Active |
| D-063 | **WP-013: E-01 reconciled with D-055g on a measured basis (zero-spread demoted to new W-07, WARNING); 2016-06-24 characterized as genuine (Brexit-referendum-result crash, not corruption); sample cap moved from accumulation to markdown-render only, archive-wide** | **Full-archive measurement, not extrapolation from the two known clusters (per this package's own requirement):** independently re-scanned all 196 months of `data/tick/` directly (not via `qa/checks.py`, as a cross-check of it) — 11 zero-spread rows and ZERO negative-spread rows across the entire 2006–2022 archive, 259,014,364 ticks total (rate 0.0000042%). This number matches the real run's own E-01 count exactly, independently confirming that check's correctness. All 11 resolve into exactly three tiny clusters: 2009-11-13 (5, D-055g's already-established case), 2016-06-24 10:14:56–57 UTC (5), and one isolated row, 2016-06-27T14:57:05.923Z, found only because this scan does not cap samples — **explicitly NOT characterized to the same standard as 2016-06-24 and not claimed as Brexit-explained**: only a brief density check was run against it (elevated tick count in its surrounding window, consistent with the continued post-referendum volatility that week, nothing more), not the same direct price-continuity/hypothesis test 2016-06-24 received. It is covered by the same W-07 finding structurally (it is a zero-spread row, and the fix below applies to the check, not to individually-approved rows), but its cause is UNCONFIRMED — a future reader should not read "2016-06-27" and assume this entry already explained it. **2016-06-24 investigated directly, hypothesis tested rather than assumed (Brexit referendum result, GBP/USD's largest single-day move in decades):** confirmed against the real surrounding ticks — the whole day carries ~2x the tick volume and ~8.6x the price range of an ordinary control Friday one week prior (2016-06-17), the specific 10-minute window around the cluster is in continuous, severe, one-directional decline (not calm before/after an isolated spike), and the five zero-spread ticks THEMSELVES form a smoothly monotonic price sequence (1.379400 → 1.379330 → 1.379280 → 1.379240 → 1.379220, each a distinct, plausible price, never a stuck/repeated value) bracketed immediately before and after by normal non-zero spreads. This is the signature of momentarily evaporated two-sided liquidity during genuine one-sided repricing, not a decode defect — the hypothesis holds, on evidence, the same discipline D-057's Flash Crash theory was held to (and which that investigation rejected; this one does not). **Reconciliation, driven by the measurement, not curve-fit to the known rows:** `_check_spread` split into two genuinely different findings — E-01 now covers ONLY strictly negative spread (`ask < bid`, a true inversion), staying ERROR; a new **W-07 "Zero-spread quote"** covers `spread == 0` at WARNING severity. This mirrors D-055g's own precedent exactly (its ingest-layer fix already drew this identical negative/zero distinction: "reject only bid > ask; a true inversion is still treated as corruption") — D-055g fixed that distinction at ingest time and had no reason to know QA's separate, independent E-01 check existed and hadn't been given the same treatment; this entry closes that gap on the QA side, on its own evidence, not by assuming D-055g's conclusion transfers. Zero negative-spread rows exist anywhere in the corrected archive, so E-01 remains meaningful as "never yet seen, and should stay rare" rather than becoming a dead check. **Sample-cap fix, applied archive-wide, not narrowly to E-01/W-07:** the old `_SAMPLE_LIMIT=10` capped every check's `Finding.sample` AT ACCUMULATION, meaning both the markdown report and its Parquet sidecar lost anything past the 10th match — this is precisely why the real run's own report could not show where its 11th E-01 occurrence was. Per D-013 (Markdown plus Parquet/CSV sidecars), a sidecar capped the same way the human-readable report is has no distinct purpose left. Fixed by moving the cap out of `qa/checks.py` entirely (`_append_sample` and every inline `_SAMPLE_LIMIT` site across E-04/E-05/E-06/W-06 (dormant), W-04/I-03, W-05/I-04, and I-01 all now collect every match, uncapped) and into `qa/report.py`'s markdown renderer only, as a new `_MARKDOWN_SAMPLE_DISPLAY_LIMIT = 10` — the markdown now also states explicitly how many samples were omitted and that the sidecar holds them, rather than silently stopping. **Verification:** new tests confirm the real 2009-11-13 and 2016-06-24 row values (the actual historical bid/ask/timestamps, not fabricated) each produce W-07 not E-01; a negative-spread regression confirms E-01 still fires at ERROR; a 25-row synthetic batch confirms `Finding.sample` is no longer capped at accumulation; an end-to-end `write_report` test confirms a 15-sample finding shows exactly 10 in markdown (plus an explicit "5 more" note) while its Parquet sidecar row holds all 15. 395 tests pass under both `pytest` and `python -m pytest`; 11/11 architecture contracts held. `docs/STAGE_1A_RUNBOOK.md` Section 5's "should not appear as an E-01/E-02 finding" claim for 2009-11 is now true under the fixed check; the runbook document itself was not edited (out of this package's file scope) — per the WP's own definition of done, the Stage 1A run resumes from Step 2 with this fix in place, Step 1's resample output untouched and not re-run | Active |
| D-064 | **WP-014: I-01's count-capping bug fixed; sibling audit found no other instance; check redesigned from "compare against a full-day expectation" to "flag unexpected nonzero activity," on investigated reasoning** | **Found by accident, not by design:** re-running Step 2 after D-063 (which only intended to uncap SAMPLE lists) changed I-01's own reported count from "10 of 33" to "33 of 33," with no data change at all. **Bug:** `_check_dst_day` was the one check in `qa/checks.py` whose `count` was computed as `len(deviated)` — the exact same list its samples were drawn from — rather than an independent counter every other check uses. Before D-063 uncapped list-appending generally, this meant I-01's reported COUNT, not just its displayed examples, had been silently capped at 10 for as long as the check existed; nobody had previously run it against a range with more than 10 flagged days. **Fixed:** `_check_dst_day` now tracks `leaked_count` as its own integer, incremented independently of the `leaked` sample list, matching every other check's shape. **Sibling audit, as required:** every other check in the file re-examined for the identical shape (count structurally derived from a list that could ever be capped) — none found. W-03's `len(flagged_idx)` and W-04/I-03's `.height` on a Polars frame are technically "list/frame lengths," but those underlying structures were never capped in the first place (W-04/I-03 already share one uncapped `flagged`/`skipped` frame for both count and sample; W-03's `flagged_idx` was always a plain, uncapped Python list) — so unlike I-01, there was never a point where their count and their displayed samples could diverge. I-01 was the only case where the count itself was silently wrong, not just its examples. **Redesign, investigated rather than reflexively copying W-05's Sunday-exclusion:** confirmed directly from `jarvis.timeengine.calendar` that every US DST transition falls on a Sunday, and a Sunday-labelled trading day's bounds (previous-day 17:00 NY through same-day 17:00 NY) sit entirely inside `is_weekend_gap`'s closure — the market being shut there is guaranteed by construction, unrelated to DST, exactly the reason W-05 already excludes Sat/Sun labels. Checked the alternative the WP asked about directly: redirecting to the following Monday does NOT work, because `trading_day_bounds(monday)` computes both of ITS boundary instants from local times already past the transition, so Monday's own duration is always a plain 24h regardless of whether the transition was handled correctly — a Monday-based check would just be a weaker, redundant copy of W-05, not a DST-specific signal. **Chosen design:** flip the comparison instead of relocating it — the correct expectation for a DST-transition Sunday's label is not "close to a full day" but EXACTLY ZERO bars, deterministically, no threshold to tune, since it is provably inside market closure; a nonzero count there is direct, meaningful evidence that `trading_day_bounds` or `bar_level_checks`' own day-bucketing search mis-located the DST-adjusted boundary for that specific transition. Checked against the real archive's actual numbers already in hand from the Stage 1A run: 0 of 33 would be flagged under the new logic — for the first time, genuine positive evidence the boundary computation is correct, not a check that fired unconditionally for a reason unrelated to what it claims to test. `I-01` now always emits one Finding whenever a DST-transition day is in range, even at count=0 — a deliberate, documented exception to every other check's "no finding when count is zero" convention, since a confirmed-clean result is itself the useful information here. **Verification:** a new test confirms the clean/expected case (zero bars on the Sunday label) still produces a Finding with count=0 and an explicit confirmation message, not silence; a new test injects bars directly onto a DST-transition Sunday's own label and confirms I-01 flags exactly that day, proving the redesign actually catches the failure mode it exists for; a new test repeats this across 12 real DST-transition days (found by `trading_day_bounds` itself, not a hardcoded list) to confirm the count-capping fix holds past the old 10-sample boundary. 396 tests pass under both `pytest` and `python -m pytest`; 11/11 architecture contracts held. `qa/report.py`'s sample-cap logic (D-063) was not touched, per this package's file scope. Per the WP's definition of done, the Stage 1A run resumes from Step 2 again — Step 1 remains untouched and clean | Active |

## 2026-09-13 — WP-015 Part A: the Stage 0 probe CLI crash is fixed

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-065 | **WP-015 Part A: `jarvis stage0 probe`'s `UnicodeEncodeError` crash fixed (a new `OutputError`, exit code 4, plus stream reconfiguration); scope confirmed by scanning every report field, not assumed narrow; runbook exit-code documentation corrected to match observed reality** | **Found by executing `docs/STAGE_1A_RUNBOOK.md` for real, not by review:** Step 4 of the actual Stage 1A run computed a genuine baseline probe result (WIDEN_CONTEXT, M=61.50, P10=51.00, `narrowest_intersection=C-D∩C-C`), wrote its report to disk successfully, then crashed printing the terminal summary — `typer.echo(f"  Narrowest          {gate_result.narrowest_intersection}")` in `cli/main.py`'s `stage0_probe` raised `UnicodeEncodeError` because this machine's console uses cp1252, which cannot encode U+2229 ('∩'). The process exited 1 — Python/Click's default uncaught-exception code — not the runbook's documented 0 or 3, because an unhandled `UnicodeEncodeError` is not a `JarvisError` and bypassed that handling entirely. **Scope confirmed by scanning, not assumed narrow, per this package's own requirement:** every source file on the CLI's output path was scanned character-by-character for non-ASCII content. `narrowest_intersection` was the only field actually reachable from a `typer.echo()` call — the four `∩`-bearing `INTERSECTION_KEYS` strings (`probe/gate.py`) also flow into `probe/report.py`'s per-year markdown table, but that is written to a UTF-8 file, never echoed to the console. One further non-ASCII character was found (`features/compute.py`'s docstring contains '§', a Technical Bible cross-reference) and confirmed NOT reachable via any CLI output — `features_build` prints only bar counts, feature-set version, month strings, feature names, and null counts, none of which is that docstring. So `narrowest_intersection` was a real, guaranteed-triggering bug on every prior and future probe invocation on this console (it is always one of the four two-context `INTERSECTION_KEYS`, never absent for a real gate evaluation) — but it was also the ONLY one; no other command carries the same risk today. **Fix, addressing the question of scope directly (should output-stage failure be handled generically, regardless of cause?) — yes:** (1) `sys.stdout`/`sys.stderr` are reconfigured with `errors="backslashreplace"` at CLI module import (guarded by `hasattr(..., "reconfigure")`), so an unencodable character degrades to a readable escape instead of crashing — verified directly against a real `cp1252`-encoded stream, not assumed from the Python docs. (2) A new `_echo_summary(lines)` helper wraps every command's terminal-summary `typer.echo()` calls and converts ANY exception there into a new `OutputError(JarvisError)` (`exit_code=4`) — this is deliberately not a narrow catch of `UnicodeEncodeError` specifically, since the underlying computation and any report file have, in every case, already completed successfully by the time output printing runs; the failure mode this documents is "the summary didn't print," not "the computation failed," regardless of what specifically broke the printing. All 6 CLI commands (`doctor`, `data fetch`, `data resample`, `data validate`, `data import-histdata`, `features build`, `stage0 probe`) were restructured to route their summary output through this helper, inside their existing `except JarvisError` block, so no command gained a new except-clause — the pre-existing handling now simply also covers this case. **Runbook corrected to match the actually-observed exit code, not the previously-assumed one:** `stage0_probe`'s docstring and `docs/STAGE_1A_RUNBOOK.md` now both document exit 4 as a real, distinct possibility ("gate computed and the report was written, but the terminal summary itself failed to print — check the report file directly") alongside the existing 0/PROCEED_* and 3/WIDEN_CONTEXT-etc. codes. **Verification:** one test reproduces the exact `cp1252`+`backslashreplace` stream shape directly (not through `CliRunner`, whose own captured stream is UTF-8 and would not exercise the real console's failure mode) and confirms `∩` prints as a readable `∩` escape rather than raising; a second test forces an arbitrary exception inside `_echo_summary` (not an encoding error specifically) and confirms it surfaces as `OutputError` with `exit_code=4`, proving the handling is generic, not a UnicodeEncodeError special-case. 399 tests pass under `pytest`; the `slow`-marked property suite passes separately; 11/11 architecture contracts held. `probe/gate.py`'s `INTERSECTION_KEYS` (the sole definition site of `∩`) was deliberately left untouched — renaming the domain model's own vocabulary to work around a console-encoding limitation would be a larger, unjustified change than fixing the actual output layer | Active |

## 2026-09-13 — WP-016: Stage 0 concludes for GBP/USD

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-024b | **Gap in D-024 found by hitting it, not by review: what happens when the single permitted widening attempt still lands in the 40–100 band. Ruling — EUR/USD fallback deferred (not triggered, not abandoned); multi-pair-simultaneous rejected outright; Stage 0 concludes for GBP/USD** | **The gap, stated precisely:** D-024 reads "Counts 40–100 trigger one widening attempt. Below 40 after widening, the EUR/USD comparison runs." It specifies the **below-40-after-widening** case (→ EUR/USD) and, implicitly, the **≥100** case (→ proceed), but says nothing about the case where the post-widening result stays **in the 40–100 band** — neither low enough to trigger the comparison nor high enough to proceed. This is not a hypothetical gap: it is exactly what happened on this project's real, first, and only-permitted widening attempt (WP-015 Part B chose the parameter and committed to it in writing before the result was known, per D-024a/C10's one-shot discipline). **The evidence, stated plainly:** baseline probe (2007–2022, 16/17 admissible years, `reports/stage0/STAGE0__20260913T141427Z.md`) — narrowest intersection `C-D∩C-C`, M=61.5, P10=51.0 → `WIDEN_CONTEXT`. The one sanctioned widening was `break_buffer_atr` (0.10→0.05, the only one of the two supported `--widen` targets with a mechanical path to `C-D∩C-C` — `range_pct_max` governs C-A, gated by `range_pct_min`, a separate field C-D does not use, so it cannot plausibly move this intersection at all; `break_buffer_atr` governs C-B, and C-C's population is structurally downstream of and bounded by C-B, per `detect_events`' own control flow). Result (`reports/stage0/STAGE0__20260913T150956Z.md`): several years' `C-D∩C-C` counts genuinely increased by 1–2 (e.g. 2020: 64→66, 2018: 66→67), confirming the parameter really was relaxed and really did move the population in the expected direction — but M stayed bit-for-bit unchanged at 61.5 and P10 moved only 51.0→51.5. Still `WIDEN_CONTEXT`. The correct lever, applied for real, barely moved the number. **Ruling on EUR/USD — deferred, not triggered, not abandoned:** the widening result is itself informative, not merely inconclusive — the one lever with a genuine mechanical path to the bottleneck moving the annual median by exactly zero suggests the scarcity of `C-D∩C-C` (a wide overnight range followed by a further breakout-continuation) is a real property of how these two contexts co-occur on GBP/USD, not a definitional artifact of where the thresholds happened to sit. EUR/USD is a genuine multi-week undertaking — its own full ingest verification, likely its own vendor-specific quirks (D-055's HistData timezone investigation alone took most of this project's engineering effort to date) — not a quick comparison check, and there is no strong reason to believe a different major pair would clear the same bar: a wide pre-London range followed by a further breakout-continuation may simply be an uncommon co-occurrence on major pairs generally, not a GBP-specific weakness. Committing multiple weeks of ingest work off an ambiguous, marginal result (M sitting mid-band, moved +0.0 by the correct lever, P10 moved +0.5) would be exactly the kind of threshold-adjacent, inconvenient-result-driven decision D-024a was written to prevent — the fact that this is now genuinely inconvenient (no clean answer either way) does not lower that bar. **The fallback is deferred, not closed off:** it remains available later on better evidence, specifically Stage 3's measured context-to-setup attrition rate once it exists — the same class of evidence D-024a already names as legitimate grounds to revisit gate numbers — never on this result alone. **Ruling on multi-pair-simultaneous — rejected outright, independent of tonight's specific number:** running GBP/USD and EUR/USD (or others) side by side is rejected on its own terms, not as a fallback to the EUR/USD question above. It directly undermines C10's multiple-testing discipline (every dev run counted, sweeps count individually — more simultaneous shots on goal is exactly the multiple-comparisons problem C10 exists to prevent) and D-007's roadmap bet (market description before strategy machinery; depth on one pair before breadth across several) — especially before Stage 2's market-description work has happened even once, on any pair. This is also already one of V1's frozen exclusions ("no multi-pair"); this ruling adds the mechanism-level reasoning, it does not introduce a new constraint. **Resolution:** Stage 0 concludes for GBP/USD. `C-D∩C-C`'s narrowness is carried forward explicitly into Stage 2 as a known, evidenced characteristic of the pair worth explaining in the market-description work — a wide pre-London range and a further breakout-continuation genuinely co-occurring less often than the gate's original 100/60 design assumed — not as a blocking failure or an unresolved question. **This is a ruling, made deliberately, with the reasoning discussed and settled before this entry was written — not a provisional placeholder subject to being revisited by a future inconvenient result; it may be revisited only the way D-024a already permits any gate-adjacent decision to be revisited: through this decision log, on new evidence** | Active |
| D-042 | **Stage 0 gate decision, finalized: GBP/USD confirmed, Stage 0 concludes** | Baseline probe (`reports/stage0/STAGE0__20260913T141427Z.md`): narrowest intersection `C-D∩C-C`, M=61.5, P10=51.0 over 16/17 admissible years (2007–2022) → `WIDEN_CONTEXT`. Single sanctioned widening, chosen and committed to in writing before its result was known (WP-015 Part B): `--widen break_buffer_atr` (`reports/stage0/STAGE0__20260913T150956Z.md`) — M unchanged at 61.5, P10 51.0→51.5 → still `WIDEN_CONTEXT`. Per **D-024b**'s ruling on the resulting gap in D-024 (D-024 never named a next step for a post-widening result that stays in the 40–100 band, which is what happened here): the EUR/USD comparison is deferred, not triggered by this result; running GBP/USD and EUR/USD simultaneously is rejected outright. **GBP/USD is confirmed as the instrument going forward, not provisionally. Stage 0 is concluded. Stage 2 (market description) is the next stage, not yet started.** `C-D∩C-C`'s narrowness — a wide pre-London range genuinely followed by a further breakout-continuation less often than the gate's 100/60 design assumed — carries forward into Stage 2 as a known, evidenced characteristic of GBP/USD worth explaining, not as an open question | Active |

## 2026-09-13 — WP-017: the vacuous single-reader-rule contract is fixed

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-066 | **WP-017: the single-reader-rule `.importlinter` contract's stale target (`jarvis.bars.storage`, which never existed) fixed to the real modules (`jarvis.bars.store` and `jarvis.bars.resample`); `allow_indirect_imports=true` added after discovering the naive fix flagged every legitimate consumer of `bars`'s public interface, not just bypasses of it; a dedicated filesystem-access test added; the mechanism confirmed, by controlled experiment, to actually catch a violation** | **Found during Stage 2 kickoff investigation, confirmed directly, not assumed:** D-010 names the single-reader rule (only `bars`/`vault` touch market Parquet) as one of exactly two rules carrying this project's architectural integrity. The `.importlinter` contract meant to enforce it targeted `forbidden_modules = jarvis.bars.storage` — a module that has never existed anywhere in this codebase; the real Parquet/DuckDB access point that was actually built is `jarvis.bars.store` (`BAR_SCHEMA`, `bars_path`, `read_bars`, `write_bars`). Import-linter silently skips forbidden targets absent from its graph, so this contract was vacuously `KEPT` — passing by matching nothing — from before Stage 0 finished until this fix; the contract's own comment already flagged this as a placeholder needing an update that never happened. **Confirmed the actual current module path before editing, per this package's own instruction, rather than assuming the obvious rename was sufficient:** `jarvis.bars.resample` (which reads tick Parquet directly via `jarvis.ingest.histdata_import.tick_path` and calls `write_bars`) is equally a real storage-access point of `bars`, re-exported through `jarvis.bars.__init__` exactly like `bars.store` is — so the target was corrected to both, not just one. **A second, non-obvious defect found only by testing the fix, not by inspection:** naively renaming `jarvis.bars.storage` → `jarvis.bars.store` in isolation made the contract fail almost universally, because `jarvis.bars.__init__` re-exports from `jarvis.bars.store` internally (so callers can legitimately write `from jarvis.bars import read_bars`), and import-linter's `forbidden` contract type checks import chains **transitively** by default — meaning `jarvis.qa.report -> jarvis.bars -> jarvis.bars.store` (a sanctioned, correct usage) was flagged identically to a genuine bypass. Confirmed directly by running the corrected contract before and after adding `allow_indirect_imports = true`: without it, `qa` was wrongly flagged for its legitimate `from jarvis.bars import read_bars`; with it, only a literal, direct `from jarvis.bars.store import ...` (or `.resample`) is caught — matching the rule's actual intent ("don't bypass `bars`'s public interface"), not the broader and wrong "no module may ever transitively cause `bars.store` to be imported." **Mechanism proven, not just trusted, per this package's own requirement:** a throwaway file was added to `jarvis.features` (a module with zero pre-existing illegal chains) containing a single deliberate `from jarvis.bars.store import BAR_SCHEMA`, the corrected contract was re-run and confirmed to report it as newly `BROKEN`, and the file was then deleted and the contract re-run again to confirm the finding reverted to only the pre-existing real violations (`git status` confirms no trace left). **A dedicated, permanent filesystem-access test added** (`tests/architecture/test_single_reader_rule.py`), matching the Bible's claim that one exists for this rule specifically (previously false — confirmed by grep, nothing matched) — distinct from `test_layers.py`'s generic "every contract passes" test, so this rule's own enforcement is independently, nameably verified: one test confirms the contract's targets are real graph nodes (guards against a repeat of the vacuous-target bug), one injects a direct import edge into a fresh graph and confirms `find_modules_directly_imported_by` (the same primitive the contract's direct-check relies on) detects it, deliberately using a module (`jarvis.qa.report`) chosen because it does NOT already import `bars.store` directly — proving the injected edge specifically is what triggers detection. **A real, newly-surfaced violation, reported per this package's own instruction rather than quietly worked around — see D-066a** | Active |
| D-066a | **Real violation surfaced by D-066's fix, then fixed as an immediate same-day follow-up: `cli/main.py` bypassed `bars`'s own public interface** | `src/jarvis/cli/main.py` imported `from jarvis.bars.resample import resample_range` and `from jarvis.bars.store import read_bars` directly (lines 14–15) — even though `jarvis.bars.__init__` already re-exports both names for exactly this purpose. This was structurally invisible before D-066, since the contract meant to catch it was vacuous. It was real: confirmed directly, twice — once via `lint-imports` reporting exactly these two lines as the only broken chains once the contract targeted the real modules, and again via the pre-existing `tests/architecture/test_layers.py::test_import_linter_contracts_pass`, which correctly failed, printing the identical two lines. Deliberately not fixed in WP-017 itself (`src/` was outside that package's file scope; a newly-surfaced violation is a finding to report, not quietly work around) and no `ignore_imports` suppression was added to hide it — the contract stayed honestly `BROKEN` in the interim. **Fixed as its own immediate follow-up:** both imports replaced with the sanctioned `from jarvis.bars import read_bars, resample_range` — the same pattern `jarvis.qa.report` and `jarvis.probe.report` already used correctly, so `cli` now matches its own siblings rather than being the one caller bypassing the interface. Confirmed `test_import_linter_contracts_pass` now genuinely `PASSED` (not merely "doesn't newly fail" — re-run directly and observed passing), and confirmed via a full run of every contract in `.importlinter` (not just this one) that all are genuinely `KEPT` under both `pytest` and `python -m pytest`. The single-reader rule is now both correctly targeted and actually clean | Active |

## 2026-09-13 — WP-018: Stage 2 kickoff — date-scoping resolved, R5 built

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-067 | **Specification correction: C4b's "Stage 2 conditional reports run on 2007–2022" and G.1.1's "Data scope: 2007–2022 by default" both conflicted with AR-1's later "Stage 2 descriptive reports run on 2007–2014" — checked directly per this package's own instruction, not assumed; the proposed R2-is-exempt reading is NOT confirmed by any categorization in the Bible; AR-1 is ruled to apply uniformly to R1, R2, and R5** | **The categorization check, done as required before resolving anything:** searched every occurrence of "conditional" across all four Technical Bible parts and the Product Bible. R2 is called "conditional" in exactly two places: its own title (`TECHNICAL_BIBLE_2.md` G.1.2, "R2 — London range conditional on pre-London range percentile") and AR-6's prose summary (`TECHNICAL_BIBLE_4.md` line 626, "R2 conditional London range") — which is itself just a shorthand paraphrase of R2's own title in a different amendment entry, not an independent categorization. **No table, section header, or milestone description anywhere categorizes reports into "conditional" vs. "descriptive" subtypes.** Worse for the proposed reading: G.1.1's own opening sentence — the same section C4b's "conditional reports" language is meant to resolve against — reads "It [Stage 2] computes **conditional descriptive statistics** about GBP/USD," using "conditional" and "descriptive" as ONE COMPOUND PHRASE describing Stage 2's output **as a whole**, not as two distinguishable report categories. Every one of R1–R6 conditions its statistics on something (year, day-of-week, hour, quintile bucket) — there is no textual basis for reading "conditional" as R2-specific. **Resolution:** AR-1 (Part 4 §Z.1, an adversarial-review amendment explicitly stated as "already incorporated," textually and chronologically LATER than C4b/PDLA-03's original "2007–2022" framing, and responding to a named risk — hypothesis generation from data then tested on it — that applies identically to R1, R2, and R5) supersedes C4b/G.1.1's earlier, more general framing for Stage 2's initial report suite. **All three of R1, R2, and R5 run on 2007–2014, not a two-tier split.** This is a genuine specification defect, logged the same way D-031/D-032b/D-033 were — a claim in the governing documents that does not survive being checked against their own other text — not a silent edit: `PRODUCT_BIBLE.md`'s C4b row and `TECHNICAL_BIBLE_2.md`'s G.1.1 were both corrected to state the 2007–2014 restriction explicitly and uniformly, with a pointer to this entry. **A second, related misreading caught in the same pass, before any code was written:** AR-1's own text — "Development experiment runs still use 2007–2018, but every development report must show 2015–2018 as a separate segment... An effect present in 2007–2014 and absent in 2015–2018 is flagged and cannot reach PROMISING" — describes a mechanism belonging to Stage 3+ **development experiment reports** (strategy backtests, gated by the `PROMISING` lifecycle state), not to Stage 2's own descriptive reports. R1/R2/R5 read 2007–2014 and stop there; they do not themselves display a 2015–2018 segment — that display belongs to a different, not-yet-built report category entirely. WP-018's own step 5 asked for R5 to show that segment; R5 was built per AR-1's literal text instead (2007–2014 only, no segment), and this deviation is disclosed here rather than silently complied with or silently corrected without a note | Active |
| D-068 | **WP-018: R5 (Spread and cost climate) built and run end-to-end against the real 2007–2014 dataset, as a deliberately minimal vertical slice — `core.bootstrap`, `describe.periods`/`describe.r5`, `reporting.furniture`/`reporting.describe_r5` — with a real performance defect found and fixed by actually running it, not by review** | **Scope, per D-019's precedent (vertical slice, not complete layers):** built only what R5 itself needs. `core/bootstrap.py`: one function, `bootstrap_ci`, a percentile bootstrap with a required `seed` (Part 2 §F.1 reproducibility). `describe/periods.py`: a single fixed `stage2_descriptive_range()` returning 2007-01-01–2015-01-01 (half-open) — not a parameter, not a new `vault.PERIODS`/`QueryClass` entry (neither `vault.GatedReader` nor any period matching 2007–2014 exists in code; building that machinery was out of this package's scope). `reporting/furniture.py`: G.1.3's watermark, sample-size note (`n<10` suppressed, `n<30` warned), closing paragraph, and header block — the last explicitly disclosing, not omitting, the fields that don't exist yet (dataset version: Stage 1B unbuilt) or don't apply to R5 (session set, feature set: R5 uses bars alone). `describe/r5.py`: pure computation over an already-loaded bars frame (mirrors `features.compute`'s own pattern — never reads `data/` itself, needing no single-reader-rule exception), grouping by hour-of-week (Europe/London local, matching this project's existing London-anchored hour convention) and by UTC calendar year, with a 60-present-bar rolling range (null until a full window accumulates, mirroring `atr_bars`' own "present bars, not clock minutes, no partial-window estimate" convention). **A real, significant defect found only by running this against real data, not by code review or synthetic tests:** the first implementation's `bootstrap_ci` materialized one `(n_resamples, len(values))` array per call. R5's synthetic-data unit tests (small samples, hundreds to tens of thousands of rows) never exposed this. The real 2007–2014 dataset's year buckets are ~350,000 observations each — a naive `(2000, 350000)` float64 array is ~5.8GB; the first real run was observed directly consuming over 1GB and climbing before being killed, twice, before the actual cause (not a hang — confirmed via accumulated CPU time, which kept climbing steadily) was isolated by profiling each pipeline stage separately against one real year of data. **Fixed two ways:** (1) chunked resampling, bounding peak memory to a fixed budget (~160MB) regardless of sample size or resample count, verified byte-identical to unchunked results at any chunk size (a dedicated regression test asserts this, and asserts the specific ~350,000-element scale that broke the naive version now completes correctly); (2) `rng.integers`+indexing in place of `rng.choice` (measurably faster for this use, profiled directly). Chunking alone fixed memory but not wall-clock time (chunking cannot reduce total resample-element operations, only bound how much is materialized at once) — a real ~350,000-element bootstrap at the library's own default `n_resamples=2000` still took ~45 seconds; profiled at 2000/1000/500/300 to find where the report's total runtime became reasonable. **`describe.r5` explicitly uses `n_resamples=500`, not the library's 2000 default** — a disclosed precision/time trade-off (still within the commonly-cited 500–2000 range for a percentile 95% CI, and R5 is explicitly exploratory per its own watermark, not confirmatory), stated in code comments, not silently chosen. **A second real defect, found only by reading the actual generated report, not by re-reading the code:** the renderer's "Calculation" section hardcoded the literal text "n_resamples=2000" — stale the moment `describe.r5` was tuned to 500, and the first real report printed a false methodology claim about its own computation. Fixed to read `confidence`/`n_resamples` from the result's own `BootstrapCI` objects (what this specific run actually did), not a string that can drift out of sync with the code that produces the data; a regression test added. **Run for real, twice** (once before, once after the renderer fix) **against the genuine, full 2007–2014 dataset** (`reports/describe/R5__20260913T191803Z.md` + Parquet sidecar): 2,802,563 bars read in 2.9s, R5 computed (176 bootstrap-bearing cells: 122 populated hour-of-week buckets — Fri 23:00 through Sun 20:00 correctly near-empty, matching real weekend market closure — plus 8 year buckets) in 274.1s. Zero cells suppressed; one warned (Fri 22:00, n=13, the weekend-open edge). A genuine, interesting finding surfaced directly, unprompted: median GBP/USD spread compresses from 5 pips (2007–2008: 0.0005) to 3 pips (2012–2014: 0.0003) over the window, with several early-year medians landing on an exactly-repeated discrete value (bootstrap CI width of zero) — real market-structure evidence of a real pattern (spreads quantized to round pip levels; liquidity/spread compression over time), not a computation artifact, and exactly the kind of thing R5 exists to surface for Stage 2.5's human hypothesis generation. 448 tests pass under both `pytest` and `python -m pytest`; 11/11 architecture contracts held, including WP-017's now-real single-reader rule — this is its first genuine test under new code, and it held without needing an exception. **No CLI wiring — see D-068a** | Active |
| D-068a | **Gap closed same day: `jarvis describe run` now exists — see D-068c** | Originally: R5 was invoked directly as a Python function, not via the CLI, since `src/jarvis/cli/` was outside WP-018's allowed files. The Technical Bible's CLI reference table (`TECHNICAL_BIBLE_4.md` §Q) names `jarvis describe run --report R1\|R2\|R5 [--year]`; R5's WP-018 end-to-end run was driven by a standalone script calling `bars.read_bars` → `describe.r5.compute_r5` → `reporting.describe_r5.write_r5_report` directly instead. WP-019 wired the real command (D-068c) before R1 started, matching D-066a's own same-project pattern of disclosing a scope-boundary gap and then closing it as an immediate, explicit follow-up rather than letting it linger | Deferred → resolved, see D-068c |

## 2026-09-13 — WP-019: n_resamples resolved with real evidence; R5 wired into the CLI

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-068b | **`n_resamples=500` confirmed correct by direct measurement, not inherited by accident from the pre-chunking memory workaround — chunked resampling is confirmed memory-bounded identically at n_resamples=2000, but 2000 buys no measurable CI precision over 500 on real R5 data, only 4x the time** | **The premise checked directly, not inferred from the chunking logic:** ran `bootstrap_ci` on a real ~350,000-element sample at `n_resamples=2000` with `tracemalloc` attached — peak Python-tracked memory 479.8MB, essentially identical to `n_resamples=500`'s 478.8MB (both bounded by `_MAX_ELEMENTS_PER_CHUNK`, exactly as the chunking design intends and exactly as the user's premise predicted). Confirmed: **500 was not still necessary for memory** — chunking already decoupled memory from `n_resamples` entirely. **The actual question — does 2000 move the CI bounds — answered on real cells, not synthetic ones:** extracted the genuine `spread_twa` arrays R5's own grouping produces from the real 2007–2014 dataset (not re-simulated) for all 8 year buckets plus 4 representative hour-of-week buckets (the smallest real bucket, n=13; two mid-sized; one of the largest), and ran `bootstrap_ci` on each at both `n_resamples=500` and `n_resamples=2000`, same seed, real numbers reported directly: all 8 year buckets — bounds identical at both resample counts to at least 4 significant figures (several exactly bit-identical; the largest disagreement, 2009, differs in the 11th significant figure, i.e. floating-point noise, not a real difference). The n=13 bucket — identical: `[0.0005000, 0.0007000]` at both. Three non-degenerate hour-of-week buckets with genuine CI width (`wd=4 hr=19`: `[0.0003734, 0.0003850]` at 500 vs `[0.0003735, 0.0003850]` at 2000; `wd=3 hr=15`: `[0.0003621, 0.0003683]` at 500 vs `[0.0003617, 0.0003683]` at 2000) — bounds agree to 3–4 significant figures, the small residual differences (~1e-7, under 0.1% of the point estimate) consistent with ordinary Monte Carlo noise between two resample counts of the same underlying distribution, not a meaningful precision gain. Timing confirmed the cost side directly too: a single real year-bucket bootstrap took ~10–12s at 500 and ~37–48s at 2000 (linear in `n_resamples`, as expected). **Conclusion, evidence-driven per this package's explicit instruction (not "500 worked and nothing broke"):** `n_resamples=500` is kept, unchanged, because the measured evidence shows 2000 provides no meaningful additional CI precision on this data — the report's actual medians are extremely stable (large real sample sizes, several genuinely quantized to discrete pip values) — while costing 4x the computation time for that non-gain. `describe/r5.py`'s `_N_RESAMPLES = 500` and its accompanying comment updated to record this as a confirmed, evidence-backed choice rather than an inherited one | Active |
| D-068c | **R5 wired into the CLI: `jarvis describe run --report R5`, closing D-068a** | New `describe_app` Typer sub-app and `describe run` command in `cli/main.py`, matching the Technical Bible's documented signature (`--report R1\|R2\|R5 [--year]`, Part 4 §Q) rather than inventing a different flag shape. `--report` accepts only `R5` today (`R1`/`R2` refused with a clear "not yet implemented" `UserError`, not silently accepted and ignored). `--year` is present in the signature (matching the documented shape) but explicitly refused with a clear error if passed — its intended behaviour for R5 is not specified anywhere in the Bible (confirmed by the same search that produced D-067), and guessing at unspecified behaviour was rejected in favour of an honest refusal. No `--from`/`--to`: R5's range is the fixed `stage2_descriptive_range()` constant, never a caller-supplied parameter, matching D-068's own design. Same underlying computation and output as the internal script WP-018 used (`bars.read_bars` → `describe.r5.compute_r5` → `reporting.describe_r5.write_r5_report`), now reachable as a real, documented command. One docstring wording fixed before commit: `§` replaced with the codebase's own established `SS` convention for Bible section references inside docstrings (`probe/gate.py`, `probe/contexts.py` already do this) — docstrings reach the console via `--help`, and `§` is exactly the class of non-ASCII character WP-015/D-065 exists to keep out of CLI output; caught by actually running `--help` and reading its output, not assumed safe. A CLI-level test (`CliRunner`, matching `test_cli_typer_behavior.py`'s existing pattern) confirms `--report R1` and `--year` both refuse with exit 1 without touching real data, and a separate integration test runs the real command end-to-end against a small synthetic dataset and confirms both the `.md` report and `.parquet` sidecar are written. **D-068a closed** — no longer a gap; status updated from Deferred to Active | Active |

## 2026-09-13 — WP-019 (R1 characterization): missing new_york_range catalog row corrected

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-069 | **Specification correction: the 19-feature catalog (Technical Bible Part 2 §F.3) had a row for `london_high`/`london_low`/`london_range` (row #10) but none for `new_york_high`/`new_york_low`/`new_york_range` — a real gap, not a naming choice, since `new_york` is a genuine defined session (`config/sessions/fx_core.v1.yaml`) and R1's own Question text names it explicitly alongside `pre_london`/`london`** | **Confirmed, not assumed, before writing this entry:** `new_york` is real (`config/sessions/fx_core.v1.yaml`: `tz: America/New_York, start: "08:00", end: "17:00"`), and R1's Question is "how large are the `pre_london`, `london` and `new_york` ranges" — all three sessions, by name. The 19-feature catalog names `pre_london_high/low/range` (row #7) and `london_high/low/range` (row #10) but nowhere names any `new_york_*` range feature — confirmed by exhaustive search, not oversight-by-assumption. **Correction:** row #10 extended to `london_high`, `london_low`, `london_range`, `new_york_high`, `new_york_low`, `new_york_range` — "Same over `london` and `new_york`" — rather than adding a new numbered row, matching the catalog's own established convention of one row bundling multiple same-shape concrete feature names (row #2 bundles `ret_5m`/`ret_15m`/`ret_60m`; row #7 already bundles three `pre_london_*` names under one row). This keeps the catalog's own "Nineteen features" count accurate on its own terms (a count of numbered rows, not concrete feature names — already true before this correction, since several rows already bundle multiple names) rather than requiring a project-wide "twenty features" rewrite for what is, in shape, the same kind of extension the catalog already makes for every other repeated-per-session or repeated-per-window feature group. Logged the same way D-031/D-032b/D-033/D-067 were — a real defect in the specification, found and corrected on its own evidence, not a silent edit | Active |

## 2026-09-14 — Independent audit response; WP-020 step 1 (row-vs-name count)

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-069a | **WP-020 step 1: "Nineteen features" (Technical Bible Part 2 §F.3) refers to 19 catalog rows, not 19 distinct output names — confirmed by direct count, not inferred** | Counted every concrete feature name across all 19 rows as originally written (before D-069): row 2 alone bundles 3 (`ret_5m`/`15m`/`60m`), row 7 bundles 3, row 10 bundled 3 (pre-D-069), row 11 bundles 3, row 12 bundles 2, row 18 bundles 2, row 19 bundles 3 — **31 distinct concrete names across 19 rows, before any WP-018/019/020 change.** "Nineteen" was therefore never a name-count under any reading of the catalog as originally written; it counts numbered rows, where one row is "one feature" even when it bundles several concretely-named outputs sharing one definition (several lookback windows of the same return, or the high/low/range triple of one session). D-069's row-10 extension (3 names → 6, adding `new_york_*`) is consistent with this existing convention, not a deviation from it — row 10 now bundles 6 names under 1 row, the same shape row 2/7/11 already have at smaller multiples. **No summary-language correction needed** — "Nineteen features" remains accurate on the row-counting reading, which is confirmed to be the only reading that was ever true. Name-count is now 34 (31 + 3 new `new_york_*` names from D-069) | Active |
| D-070 | **Architecture-contract vitality classified: of 11 contracts reporting "KEPT," 5 are genuinely live, 2 are weak (real source, only empty-skeleton targets), and 4 are dormant (source module itself is an empty skeleton and structurally cannot violate anything) — the "11/11 KEPT" headline has been cited as stronger assurance than it currently provides, including in this project's own prior reports** | **Found by an independent, external audit** (`docs/AUDIT_stage2_independent_20260913.md`, run from a clean clone at commit `5c63121`, separately from the session that wrote the code), **verified directly rather than taken on trust:** re-derived the classification independently by checking which of the 11 modules in each contract's `source_modules`/`forbidden_modules` are real (contain code) versus empty skeletons (docstring only) — confirmed 11 modules are still empty skeletons (`provenance`, `strategies`, `strategy_impls`, `opportunities`, `execution`, `experiments`, `vault`, `backtest`, `statistics`, `robustness`, `forward`; `describe`/`reporting` are no longer in this set, populated by WP-018/019) — this independently reproduces the audit's classification exactly: **LIVE** (5) — Five-layer architecture, purity rule, single-reader rule, single-reader: ingest cannot read bars, features cannot depend on bars directly. **WEAK** (2, source is real code but every forbidden target is an empty skeleton, so nothing exists yet to actually violate against) — bars cannot depend on vault; **probe must not compute profitability**. **DORMANT** (4, source module itself is empty, so it structurally cannot import anything, forbidden or not) — provenance depends on nothing but core; strategies cannot depend on execution/experiments/statistics/vault; execution cannot depend on strategies/opportunities; vault depends on nothing beyond core/bars/experiments.ledger. WP-017's fix addressed one mechanism of vacuity (a forbidden target that didn't exist at all); this is a second, distinct mechanism (a contract whose only weakness is which modules are still unbuilt) — this is expected and not itself a defect (pre-registering a contract before its module exists is good practice and it goes live automatically once that module is built), but the aggregate "11/11 KEPT" number has been cited, including in this project's own reporting, as if it were uniform assurance. **The one worth naming specifically:** `probe must not compute profitability`'s own comment states "this contract makes that mechanical rather than a matter of discipline" — today it does not, since all four forbidden targets (`backtest`, `statistics`, `robustness`, `execution`) are empty; probe could compute an expectancy-shaped quantity inline and nothing would catch it. This sits directly under D-042, which relied on probe's frequency-only discipline for its own trustworthiness — not evidence anything is wrong, but the absence, today, of the mechanical guarantee D-042 was described as having. **Resolution, per the audit's own suggested (and accepted) remedy — not a new contract, a classification record:** this entry is that record. Future contract-status reporting (test output, `HANDOVER.md`, any review) should read "5 live, 2 weak, 4 dormant" rather than the bare aggregate when precision matters, and this classification should be re-derived (not assumed frozen) as each dormant module gets built — `describe`/`reporting` already moved out of the empty set this way during WP-018/019, before this entry was even written | Active |
| D-071 | **Named as a pattern, not left as two unrelated per-package footnotes: two independent research-integrity boundaries — the vault boundary (D-061a) and R5's AR-1 hypothesis-generation range restriction (`compute_r5`) — are both enforced only at the CLI entry point, not structurally** | **Found by the same independent audit.** D-061a already discloses, on its own, that `resample_range`/`run_checks`/`compute` have no vault-boundary check inside themselves — only `cli/main.py` calls `reject_vault_range`. `describe.r5.compute_r5(bars, *, start_ns, end_ns)` is the same shape: its own docstring states it "does not re-filter by range itself... and trusts its caller," and the 2007–2014 restriction (D-067) is enforced only by `cli/main.py`'s `describe_run` calling `stage2_descriptive_range()` before passing bars in — a direct Python call to `compute_r5` could compute R5 over 2015–2018, the exact window AR-1 exists to keep unseen from Stage 2. Both instances are individually correct and were each honestly disclosed at the time (D-061a; `compute_r5`'s own docstring) — the concern named here is the trend, not either instance alone: two separate integrity boundaries now rest on "the CLI happens to call the right function first," and R1/R2 (and any future Stage 2 report) will inherit the identical shape by default unless a structural alternative is deliberately built. AR-5's original reasoning for the vault boundary — a capability/type-level property outlasts convention, which decays — applies identically to the AR-1 range restriction. **Not resolved here, deliberately:** `vault.GatedReader` does not exist yet (still an empty skeleton), and building it is far outside any Stage 2 report package's scope. This entry exists so that when `GatedReader` (or an equivalent) is eventually built, the decision of whether it becomes the single structural answer for BOTH the vault boundary and Stage 2's date-scoping is made deliberately, with both instances named together, rather than `GatedReader` being scoped only to its originally-stated vault purpose and this second instance being rediscovered independently later | Active |
| D-072 | **Decision-log numbering gap at D-034 noted as an unused drafting artefact, not a lost or redacted decision** | **Found by the same independent audit, confirmed directly:** numbering runs D-001 through D-069(a) with exactly one skip — no D-034 exists between D-033a and D-035, every other number present, unique, and correctly cross-referenced. No content or git history suggests a decision was made and then removed; most likely a renumbering artefact from an early editing pass. Recorded here so a future reader searching for D-034 finds this note instead of assuming something was lost | Active |

## 2026-09-14 — WP-020: session_terminal masking corrected project-wide (D-073)

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-073 | **`apply_session_terminal_mask`'s semantic corrected project-wide: a session_terminal feature now reveals the most recently COMPLETED instance of its own session, not "null until my own trading day's own window closes, then constant for the rest of that day." Found while building R1's `new_york_range`, but this is a general correction to shared framework code, not a `new_york`-specific patch — `new_york_range` becoming buildable, and a real, independent day-attribution bug in `pre_london_range_pct` this change exposed and fixed, are both consequences of it, not the headline** | **The gap, found by building against it, not by inspection:** `new_york`'s session (`config/sessions/fx_core.v1.yaml`) closes at 17:00 `America/New_York` — the exact same instant this project's trading-day convention rolls over (also anchored to 17:00 NY). Confirmed directly: `session_set.window("new_york", d).end_ns == trading_day_bounds(d)[1]` for every `d`. Under the old masking rule ("null before `ts < window_end`, constant for the rest of THAT trading day"), every bar of trading day `d` satisfies `ts_utc_ns < trading_day_bounds(d)[1]` by construction (half-open), so `new_york_high`/`new_york_low`/`new_york_range` would be masked null on literally every bar of every trading day, forever — not a bug in any feature's own `compute()`, a gap in the shared masking rule itself, invisible until a `session_terminal` feature was ever built against `new_york` specifically (nothing had been, before R1/WP-020). **Verified before implementing, not assumed:** built a real two-day fixture and confirmed directly that the pre-existing code shows null — not a prior day's value — on a new day's bars before that day's own `pre_london` window closes, proving this was a genuine behavioural gap for `pre_london`/`london` too (the existing 5 regression tests never covered a day boundary; their fixtures are single-day only), not a pure generalisation that was already true. **Fix:** `apply_session_terminal_mask` (`features/base.py`) recovers one value per trading day from the feature's own per-bar broadcast, forward-fills across days with no bars of their own (a market-closed day never masks an otherwise-valid answer), and reveals, per bar, either that day's own now-closed value (if `ts >= that day's own window_end`) or the nearest earlier day's already-closed value otherwise — null only when no instance has ever closed yet. For `pre_london`/`london` this reproduces the exact prior "after my own close, same day" behaviour and adds real coverage for "before my own close" (previously untested, now null only on the very first day in a frame, correctly). For `new_york`, every bar of trading day `d` now correctly shows day `d-1`'s completed value — buildable for the first time. **Bible corrected to match, the same way D-031/D-032b/D-033/D-035b were:** Part 2 §F.2's own text said "constant thereafter for the remainder of the trading day" — the OLD, now-incorrect semantic, written into the spec itself, not just the implementation. Corrected in place with a pointer to this entry, not silently preserved because it was already written down. **A real, independent ripple found only by implementing this, not anticipated in the original characterization:** `pre_london_range_pct_compute` extracted "each day's own value" by scanning its (masked) input series for the first non-null bar of that day — safe under the old semantic (null/non-null transitions used to coincide exactly with day boundaries) and silently wrong under the new one (a day's early bars are now legitimately non-null, showing the PRIOR day's value, so "first non-null" now picks up yesterday's value instead of today's, shifting every day's attribution by one). Confirmed as a real defect, not a hypothetical: one existing test (`test_range_pct_tie_uses_strict_less_than`) failed with a wrong-but-plausible number (51/60 instead of 50/60); two others (`test_range_pct_excludes_today`, `test_range_pct_null_below_60_prior_days`) kept passing throughout, confirmed by direct construction to be genuinely insensitive to the shift for their specific fixture values, not by luck this time noticed — the same "passes without detecting the real failure mode" shape as D-028. **Fixed by anchoring, not inferring:** `pre_london_range_pct_compute` now finds each day's own window-close instant independently (`session_window_bounds`, the same source the masking logic itself uses) and reads the value at the first bar at-or-after that instant, having independently confirmed that day actually had bars in its own window at all (a day with none is still correctly skipped, matching the feature's own documented "no bars this day -> not eligible" rule) — decoupled from whatever the masking semantic happens to be, including if it changes again. **Sibling audit, as required before calling this resolved:** searched all of `features/` for the same first/last-non-null-scan shape (`drop_nulls`, `.first()`, `.last()`, `group_by`+`.agg()`); `pre_london_range_pct_compute` was the only instance found, now fixed — nothing else in `features/base.py` or `features/compute.py` does this either. **A second real, unanticipated consequence, also found only by running the full suite, not by review:** the auto-generated leakage harness's L-3 check (`test_l3_session_terminal_nulling`, Part F §F.4/WP-007 — "the most important deliverable," parameterised automatically over every `session_terminal` feature) asserted the exact old invariant ("always null before my own day's close") and failed for all 9 session_terminal features once the semantic changed. Confirmed this was the test's own now-stale assumption, not genuine leakage newly introduced, two ways: L-1/L-2 (truncation and shuffled-future invariance, the actual general anti-leak checks) kept passing throughout for every one of these features; and `test_harness_catches_deliberately_leaky_feature` (the harness's own self-test, proving it still catches a real planted leak) also kept passing after L-3's redesign. **L-3 redesigned, not weakened:** replaced with `test_l3_session_terminal_same_day_causality`, which reuses L-1's own truncation-invariance primitive at one representative before-close bar per trading day in the fixture (not just L-1's single arbitrary fixed point) — proving comprehensively, per day, that extending the fixture with more of that SAME day's own bars never changes a value already computed before that day's own close, which is the actual property that matters; plus `test_l3b_session_terminal_null_at_true_start_of_history`, preserving the one case that is still, correctly, unconditionally null — before the very first trading day in a dataset has its own first session-close, with no prior day to draw from at all. **New, decisive test added, not just re-passing ones trusted:** `test_range_pct_day_attribution_not_shifted`, built so a one-day attribution shift produces a different, precisely-predicted wrong number (59/60 vs a shifted 60/60) rather than a coincidentally-matching one — confirmed directly, by temporarily reinstating the old extraction logic in isolation, that this specific test fails exactly as predicted (1.0 instead of 0.9833) under the bug, proving it is actually decisive rather than assumed to be. The two previously-coincidental tests were not rewritten, only annotated explaining precisely why they never distinguished correct attribution from the bug. **Verification:** 503 tests pass under both `pytest` and `python -m pytest` (up from 453 before this package); 11/11 architecture contracts held. R1's own build (paused pending this) resumes from here | Active |

## 2026-09-14 — WP-020: R1 (Session range anatomy) built

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-074 | **R1 built: `describe/r1.py` (computation, via the sanctioned `jarvis.features.compute` path for `{session}_range`/`atr_bars`, using the same anchored-extraction pattern D-073 required, not a null-scan), `reporting/boxplot.py` (hand-rolled SVG, stdlib+numpy only, no new dependency), `reporting/describe_r1.py` (renderer/writer), wired into `jarvis describe run --report R1`. Two real judgment calls made in the absence of explicit spec text, logged per WP-019's own instruction rather than left as silent implementation choices** | **Scope, mirroring `describe.r5`'s own precedent:** by-year and by-weekday are two separate marginal breakdowns per session (not a joint cross-tab), matching R5's own "by hour-of-week; by year" shape and the literal calculation text ("distribution of each session's range in price and in ATR units; by year; by day of week" — three semicolon-separated items, not a request for a cross-tab). **Judgment call 1 — ATR-unit box plot, not price-unit:** the spec's Output line ("table + box plot by year") does not say which unit the plot itself uses; the table carries both. Chose ATR units because GBP/USD's absolute price level drifted materially over 2007–2014 (~2.00 in 2007 vs the sub-1.30s by the end of the window), so a price-unit box plot would show that drift, not genuine range-relative-to-volatility change — the thing the Question ("how large... and how has that changed?") is actually asking about. **Judgment call 2 — one shared SVG with three side-by-side panels, own y-scale per panel, not three separate files or one shared axis:** the three sessions have structurally different typical magnitudes (`pre_london`'s compressed low-activity window vs. `london`/`new_york`'s active-hours sessions); a single shared y-axis would visually flatten the smaller one, and three separate files would multiply the sidecar count for what a reader will always want to compare side by side. **Delivery mechanism, per WP-019's own characterization, executed as characterized:** the box plot's five-number summary (Q1/median/Q3/Tukey whiskers/outliers) is computed directly from `numpy.percentile` — no plotting library anywhere in `pyproject.toml`'s dependency list, none added. **A real, non-obvious finding caught by testing, not assumed:** a fixture spanning exactly 10 fresh trading days showed 9, not 10, eligible days per session-year cell — traced directly to `atr_bars(n=1440)`'s own warmup (1440 one-minute bars = exactly one trading day), which every session's own window-close instant on day 1 falls inside (`pre_london` closes at minute 480, `london` at 990, `new_york`'s day-1 close-anchor at 1320 — all < 1440) — not a bug in R1's own day-attribution logic, confirmed by tracing the exact mechanism rather than adjusting the code until the test passed. **Verification:** 529 tests pass under both `pytest` and `python -m pytest` (up from 503); 11/11 contracts held. **Unlike R5, `n_resamples` needed no tuning down from `core.bootstrap`'s own 2000 default** — R1's cells are trading-day granularity (n in the low hundreds per cell: ~258 per session-year, ~415 per session-weekday, pooled over 8 years), not R5's per-bar granularity (up to ~350,000 per bucket), so the same memory/time pressure that justified D-068b's 500 for R5 never arose; confirmed directly, not assumed, by the real run's own elapsed time. **Run for real against the full 2007-2014 dataset** (`reports/describe/R1__20260914T190333Z.md` + Parquet + SVG sidecars): 2,802,563 bars, 3 sessions × (8 year cells + 5 weekday cells), 31 seconds total. The box plot rendered and was visually inspected directly, not just structurally asserted — three legible panels, own y-scale each, outliers correctly marked. A genuine, unprompted finding, visible directly in the real table: `london`'s ATR-normalized range (~30-36 across years) is consistently larger than `pre_london`'s (~17-22) and `new_york`'s (~24-31), as expected for the active session versus the compressed pre-open window and the session that opens after London's own peak volatility — exactly the kind of market-structure signal R1 exists to surface for Stage 2.5 | Active |

## 2026-09-14 — WP-021: R2 (London range conditional on pre-London range percentile) built

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-075 | **R2 built: `describe/r2.py` (computation, via the sanctioned `jarvis.features.compute` path for `london_range`/`atr_bars`/`pre_london_range_pct`, reusing the D-073 anchored-extraction pattern for all day-level values), `reporting/describe_r2.py` (renderer/writer, per-year table adjacent to the pooled table, one combined table, not eight), wired into `jarvis describe run --report R2`. Two required confirmations resolved against the literal Bible text BEFORE the renderer was written, per WP-021's own definition of done, not inferred silently afterward** | **Confirmation 1 — quintile method:** "quintile" is a defined statistical term (five equal-count groups), not five equal-width bands of the percentile's own value range, so the equal-count-vs-fixed-cutpoint question is resolved by correct use of the word already in the spec text, not an inference from silence. Two things genuinely were inferred and flagged as such before building, both confirmed by the user: (a) bucket boundaries are computed ONCE, pooled across the whole 2007-2014 period, then reused unchanged for the per-year breakdown — re-bucketing per year would make "bottom quintile" locally relative and defeat the Restriction's own artefact-check (comparing the SAME bucket definition across years); (b) tie handling, which turned out to be the load-bearing part of this confirmation. **A real, non-obvious discovery made by reading `pre_london_range_pct_compute` directly rather than assuming a roughly-continuous variable:** it is `sum(prior_60_days_with_strictly_lower_range) / 60`, so it takes only 61 possible values — with ~2,000 pooled eligible days, ties at a would-be quintile boundary are the normal condition (≈33 days per unique value against a ≈400-day bucket target), not an edge case. `_bucket_quintiles` never splits a run of days sharing an identical value across two buckets; it fills buckets in ascending value order and closes a bucket once its running count reaches ~n/5, at a value-group boundary — the user endorsed this explicitly over an arbitrary secondary sort forcing exact n/5 splits, "the same spirit as refusing to force `detect_column_order` to pick an answer it doesn't have, or D-055g treating genuine zero-spread quotes as real." **Demonstrated, not just described, before relying on it:** hand-built adversarial fixtures (a single tied group covering more than one bucket's target share) show the algorithm can leave trailing buckets genuinely empty (n=0), not merely "somewhat smaller" — this is real, tested behaviour (`test_bucket_quintiles_large_tied_group_can_starve_trailing_buckets`), not a hypothetical worst case assumed away. **Confirmation 2 — layout:** confirmed literal, not inferred — the Restriction text states outright that "the report must display the per-year table adjacent to the pooled table"; both tables render in one flowing markdown document, "By year" immediately following "Pooled" with nothing interleaved (`test_markdown_has_pooled_and_by_year_tables_adjacent`), matching R1's established single-document convention. No new chart type was needed or added. **Real-data outcome of the tie-handling choice, reported plainly per the user's own follow-up request rather than only if it looked bad:** against the full 2007-2014 dataset, the five pooled buckets came out n = 413, 412, 411, 415, 361 (Q1..Q5) — the smallest bucket (Q5, the highest-pct/least-compressed group) is real but only modestly smaller (≈13% below the other four's average), nowhere near the severe starvation the adversarial unit-test fixtures demonstrate is structurally possible; real market-derived percentile data did not produce that pathology here, and every bucket's own realized `pre_london_range_pct` range and `n` are shown identically in both tables, Q5 included, with no special-casing. **A genuine, unprompted descriptive finding, stated honestly rather than forced to match the Bible's own fictional worked example (K.3):** pooled median ATR-unit London range across the five quintiles is 32.20 / 33.65 / 32.98 / 31.53 / 32.65 — no monotonic trend from most- to least-compressed pre-London range, and the bootstrap 95% CIs overlap substantially across all five buckets; this report does not itself reproduce the illustrative "bottom quintile ~0.15 ATR above the top quintile" pattern the Bible's hypothesis-template example describes (that example was always a fictional illustration of the template's shape, not a claim about this dataset). **Verification:** 554 tests pass under both `pytest` and `python -m pytest` (up from 529); 11/11 contracts held. **Run for real against the full 2007-2014 dataset** (`reports/describe/R2__20260914T192947Z.md` + Parquet sidecar): 2,802,563 bars, 5 pooled buckets, 8 year rows × 5 buckets, 5 seconds total. This closes Stage 2's original three-report milestone (2C) — R1, R2, R5 all built, tested, and run against real data | Active → Q3/Q4 n transposed in this entry's own prose, see D-076 |

## 2026-09-15 — Overnight deep audit: D-075 transcription error resolved, gaps surfaced

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-076 | **D-075's own prose transposed Q3 and Q4's pooled bucket counts: it reads "n = 413, 412, 411, 415, 361 (Q1..Q5)" — the correct figures, verified three independent ways below, are 413, 412, **415, 411**, 361. Q3 and Q4 are swapped. Nothing else in D-075 is affected: the pct ranges, median ratios, total (2012), and every other figure in that entry are correct** | **Three independent primary-evidence checks, not one, per this audit's own "reconstruct from primary evidence" standard:** (1) the real, on-disk generated report `reports/describe/R2__20260914T192947Z.md` (gitignored, produced by WP-021's real run, never edited since) itself reads Q3=415/Q4=411 in its Pooled table; (2) the chat message sent to the user immediately after that run, quoting the same report, also correctly said "413, 412, 415, 411, 361"; (3) a fresh, independent reproduction run tonight, from the current committed code (`4e2e18b`) against the real on-disk 2007-2014 dataset (`compute_r2` called directly, bypassing the CLI and any cached artifact), reproduced bit-for-bit identical figures: Q1=413, Q2=412, Q3=415, Q4=411, Q5=361, total 2012. All three agree with each other and disagree only with D-075's own written prose. **Root cause: a manual transcription slip made only once, while composing D-075's decision-log text** — not a code defect (the renderer, the Parquet sidecar, and the CLI summary line were never involved in producing the wrong figure; grep confirms "411, 415" appears nowhere except D-075's own prose), not a stale artifact (only one R2 run has ever existed on disk), and not a chat-transcription error (chat had it right). Per this log's own append-only rule, D-075's text is not edited in place; this entry is the correction of record and D-075's Status now points here. **No other figure in D-075 is affected** — the pct ranges (0.000-0.167 / 0.183-0.383 / 0.400-0.600 / 0.617-0.817 / 0.833-1.000), the median ratios (32.20 / 33.65 / 32.98 / 31.53 / 32.65, listed in D-075 in Q1..Q5 order and already correct since they were never re-ordered), the "~13% below average" characterization of Q5, and the "no monotonic trend" finding all stand as written and are re-confirmed by tonight's independent reproduction. **Context:** found during an overnight autonomous audit session (user-directed, no new research decision made) that independently reconstructed Jarvis's state from primary sources rather than inheriting prior chat summaries, per the user's own explicit instruction that a repository/narrative disagreement is itself a finding, not something to silently reconcile | Active |

## 2026-09-15 — Overnight hardening package: verification-only fixes, no research decisions

| # | Decision | Rationale | Status |
|---|---|---|---|
| D-077 | **`day_range / day_atr`'s exact-zero-ATR case fixed in both `describe/r1.py` and `describe/r2.py`: the mask that excludes an invalid day now uses `np.isfinite(ratio)` instead of `~np.isnan(ratio)`, so a day with `day_atr == 0.0` exactly is excluded the same way a NaN day already was, instead of silently surviving as `+inf`** | **The gap:** `day_range / day_atr` gives `+inf` for nonzero/0.0 in numpy, not `NaN` — `~np.isnan(...)` does not catch it, and both files' own comments ("NaN/0 propagates harmlessly") were factually wrong for this specific case. **Checked directly against the real 2007-2014 dataset before fixing, not assumed:** zero occurrences of `day_atr == 0.0` exactly at any of the 2,071-2,074 valid day-close bars R1 extracts across all three sessions, and zero at R2's own (independent) `london` extraction. This has never actually happened to a real result. **Fixed anyway** — an empirically-true-so-far invariant that is not provably always true is exactly the shape of fragility this project has repeatedly chosen to close rather than lean on (D-055's DST work, D-057's dedup gap, D-073's masking correction all share this same "don't rely on it just because it's held so far" reasoning). **Verified as a genuine no-op against real data, not assumed:** re-ran both `jarvis describe run --report R1` and `--report R2` against the full 2007-2014 dataset after the fix; R2's output is byte-identical to the pre-fix report (diff exit 0, ignoring the Generated/Code SHA header lines); R1's only diff is the SVG filename, which is timestamp-derived. **Regression tests, each proven decisive before being trusted, not just written and assumed to work:** `test_exact_zero_atr_day_excluded_not_infinite` in both `tests/unit/test_describe_r1.py` and `tests/unit/test_describe_r2.py`, using a constructed fixture (bid bit-identical on every bar in the whole fixture, so `atr_bars`' true-range input is exactly 0 forever; ask fluctuates on a target day, so that day's mid-based session range is genuinely nonzero) — confirmed by direct inspection, before relying on either test, that the OLD `~np.isnan`-only logic actually produces `+inf` on that exact fixture (not merely a fixture that happens to pass either way). One design note: the calendar-weekday counter used to build these synthetic fixtures and the real 17:00-America/New_York-anchored trading-day index the code actually uses are offset from each other by a data-dependent amount — R2's first attempt at a single hand-picked fluctuation day landed on a trading day that turned out NOT to be the fluctuating one, silently producing a vacuously-passing test; caught only by explicitly checking, the same way D-073's decisive test was proven decisive, and fixed by widening the fluctuation span rather than by guessing a corrected offset. **Verification:** 558 tests pass under both `pytest` and `python -m pytest` (up from 554); 11/11 contracts held | Active |
| D-078 | **`describe_r1.py`'s box-plot rendering (`_box_plot_svg`) now excludes a session-year using the exact same `is_suppressed` (n<10) gate the adjacent table already uses, instead of the looser "any non-empty `raw_atr_values`" (n>0) rule it had before** | **The gap:** a cell the table would render as `n<10 suppressed` could still draw a box next to it, since the two used different thresholds. **Checked directly against the real 2007-2014 dataset before fixing:** the minimum `n` across every real session-year cell is 257 — no cell has ever had `0 < n < 10`, so this has never affected a real report either. **Fixed anyway, same reasoning as D-077** — an unproven invariant, not a provable one. **Verified as a genuine no-op against real data:** the regenerated box-plot SVG is byte-identical to the pre-fix one for the real dataset (`diff` exit 0). **Regression test:** `test_box_plot_excludes_a_year_the_table_would_suppress` in `tests/unit/test_reporting_describe_r1.py`, a synthetic `R1Result` with one n=5 year and one n=15 year for the same session, confirming the SVG contains the n=15 year's label and not the n=5 year's | Active |
| D-079 | **`describe_r5.py`'s hour-of-week table now discloses both sample sizes when they diverge: the `n` column header is relabelled "n (spread)", and the Note column states `range n={X} (spread n={Y})` for any cell where `range_60m_ci`'s own sample size differs from the row's own `n`** | **The gap, already found and quantified during the prior overnight audit, fixed here:** the displayed `n` counts only the spread sample; `_range_60m`'s own 60-bar rolling-window warmup drops its own nulls separately, so its actual sample size could differ from `n` without anything in the rendered table disclosing it. **Re-verified fresh against the real dataset before fixing, not cited from the prior figure:** the actual current gap is identical to what was previously found — 2 of 122 hour-of-week buckets diverge, maximum gap 42 of 22,890 rows (0.18%), both confined to Tuesday 01:00-03:00 London, the dataset's first calendar hours (`atr`-warmup-shaped, structurally the same "start of history" case D-073b/D-074 already found elsewhere). **Fix is a display-clarity change, not a computation change** — `HourOfWeekRow`'s own fields, `compute_r5`, and every number in the table are byte-for-byte unchanged; only the rendered Note text and one column header changed. **Verified against real data:** the two real affected rows (Tue 01:00, Tue 02:00) now show `range n=22848 (spread n=22890)` and `range n=22417 (spread n=22434)` respectively, matching the re-verified figures exactly; every other real row shows no divergence note. **Regression test:** `test_divergent_spread_and_range_n_render_both_distinctly` in `tests/unit/test_reporting_describe_r5.py`, a synthetic two-row `R5Result` (one row with matching n's, one with n=100 vs n=80) confirming the divergent row's rendered line states both numbers and the matching row's does not | Active |
| D-080 | **Sibling audit of `describe/`+`reporting/` for the same three anti-pattern shapes (the isinf gap D-077 fixed, the null-scan-as-day-value-extraction shape D-073 originally fixed, the suppression-threshold-mismatch shape D-078 fixed) found no further instances beyond the three already fixed tonight (D-077/D-078/D-079) — reported here as a clean result, not left silent** | **Anti-pattern 1 (uncaught inf):** every division in `describe/`/`reporting/` checked. `r2.py`'s `target = n / N_BUCKETS` divides by a nonzero constant, safe. `r5.py`'s `mid = (bid_c+ask_c)/2.0` divides by a nonzero constant, safe. `r5.py`'s `ratio = spread_ci.point_estimate / range_ci.point_estimate` already has an explicit `range_ci.point_estimate != 0.0` guard immediately before it, safe as originally written. `boxplot.py`'s two potential division points — `(v - y_min) / (y_max - y_min)` and `slot_width = plot_width / len(boxes)` — are BOTH already defended: `if y_min == y_max: y_min, y_max = y_min - 1.0, y_max + 1.0` runs before the first, and `if not boxes: return lines` runs before the second, both pre-existing, neither added tonight. **Anti-pattern 2 (null-scan day-value extraction):** `r5.py`'s `.drop_nulls()` (filtering nulls out of a pooled per-hour-of-week sample before computing its distribution) and `.group_by(...).agg(...)` (collecting every value into a list, not selecting one representative value) are both confirmed NOT instances of this anti-pattern — neither infers "which day/period a value belongs to" from nullness; they only affect a bucket's sample size, which is exactly D-079's finding, a different and already-fixed issue. `r1.py`/`r2.py`'s own `_extract_day_values` functions ARE the anchored-extraction pattern (D-073's fix, reused correctly), not an instance of the anti-pattern. No other instance found anywhere in either module. **Anti-pattern 3 (suppression mismatch):** `describe_r2.py`'s `BucketStats` fields (n, pct_min/max, median_ratio, q1/q3_ratio) are all derived from the same single masked array per bucket, by construction, so no divergence is possible; `describe_r1.py`'s `RangeStats` fields and `raw_atr_values` are likewise derived from one shared `valid` mask, confirmed by the pre-existing `test_raw_atr_values_length_matches_n`. No other instance found. This entry closes item 4 of tonight's work package with a genuinely clean result on the remaining ground, not a partial one left implicit | Active |
| D-081 | **Stage 2 roadmap milestone 2A ("remaining features to complete the 19") was never completed before 2C (Reports R1/R2/R5) — only 6 of the Bible's 19 feature-catalog rows are registered (`atr_bars`, `rv_60m`, the `pre_london`/`london`/`new_york` extreme+range rows, `pre_london_range_pct`), the exact subset R1/R2/R5 actually needed. The other 13 rows (`ret_1m`/`ret_5m,15m,60m`, `true_range_1m` as its own feature, `rv_ratio`, `pre_london_close_position`, `prev_day_high/low/close`, `prev_week_high/low`, `dist_to`, `time_since_session_start_ns`, `session_state`, `break_state`, `reentry_state`, `spread_now`/`spread_pct`, `hour_of_day_london`/`day_of_week_trading`/`minutes_into_trading_day`) remain unbuilt. Stated as fact, per D-045a's standard that a deviation from the stated plan needs a decision-log entry, not silence — not a recommendation to build the rest now** | **The roadmap (Technical Bible Part 4 §U, Stage 2) lists 2A before 2C** in table order, implying "remaining features" would be finished before the reports that (per the roadmap's own 2C row) depend on them. **What actually happened:** R1/R2/R5 were each built against only the features their own literal spec text required, verified directly by reading `describe/r1.py`, `r2.py`, `r5.py` and `src/jarvis/features/library.py`'s registry (12 `register()` calls, 6 of the Bible's 19 catalog rows by row-count, matching D-069a's row-vs-name-count resolution). **This does not affect R1/R2/R5's own correctness** — confirmed directly: `describe/r5.py` reads `spread_twa` as a raw bar column via `_add_derived_columns`, never through a `spread_now`/`spread_pct` feature that doesn't exist; R1 and R2 use only the 6 registered rows, all present. **No decision-log entry previously recorded this deviation from the roadmap's stated ordering** — found during the prior overnight audit (see D-076's own session), logged here on its own as this item's own entry rather than folded into another. **Whether the remaining 13 rows matter before any Stage 3 work is explicitly out of scope for this entry** — a research/planning decision for the user, not resolved or recommended here | Active |
| D-082 | **The independent-audit contract-vitality classification from D-070 (5 live, 2 weak, 4 dormant of the 11 `.importlinter` contracts) is re-verified, unchanged since D-070, and restated here as the standing convention: any future report of contract status should cite "5 live / 2 weak / 4 dormant," not the bare "11/11 KEPT" aggregate, until the classification changes** | **Re-derived independently tonight, not assumed carried forward from D-070:** every one of the 11 modules D-070 found empty (`provenance`, `strategies`, `strategy_impls`, `opportunities`, `execution`, `experiments`, `vault`, `backtest`, `statistics`, `robustness`, `forward`) checked again directly — each still contains exactly 0 lines of code outside its own `__init__.py` (`provenance/__init__.py` itself is a 1-line stub; every other listed module's directory holds only an empty `__init__.py`). No module has moved out of this set since D-070 was written (`describe`/`reporting`, which D-070 already noted had moved OUT of the empty set during WP-018/019, remain populated; nothing built since — WP-020, WP-021, and tonight's own fixes — touched any of the 11 still-empty modules). **Not a new contract, not new enforcement** — per the task's own scope, this is documentation only: the same 5 live / 2 weak / 4 dormant split D-070 already established, confirmed current and restated as the ongoing convention D-070 itself called for ("should be re-derived, not assumed frozen, as each dormant module gets built") | Active |
| D-083 | **FOUND, NOT FIXED — flagged for the user's own decision, not resolved here: `probe/contexts.py`'s `_day_value` (WP-008, never touched since) reintroduces the exact null-scan-as-day-value-extraction anti-pattern D-073 fixed in `features/library.py`'s `pre_london_range_pct_compute` — against the same masked session_terminal columns, feeding Stage 0's gate arithmetic directly** | **Found by tonight's whole-codebase sibling audit** (this package's item 6, scoped wider than D-073's own sibling audit, which was explicitly limited to `features/` and never looked at `probe/`). `_day_value` (`probe/contexts.py:56-70`) extracts "day D's own value" for `pre_london_high`/`pre_london_low`/`pre_london_range_pct` via `group_by(day).agg(drop_nulls().first())` — its own docstring claims this "mirrors `jarvis.features.library.pre_london_range_pct_compute`'s own day-value extraction," which was true when this function was written (WP-008, before D-073) but has been FALSE since D-073 changed that mirrored function to the anchored-extraction pattern instead. **The mechanism, worked through directly:** under D-073's current masking semantic, a day's EARLY bars legitimately show the PRIOR day's already-completed value (not null), so the group's chronologically-first non-null bar is now, for every day past the very first in a dataset, the carried-forward PRIOR day's value — `.drop_nulls().first()` per day-group therefore systematically returns yesterday's value, not today's, for every affected column, on every day. **Historical impact assessed, not assumed:** `probe/contexts.py` has not been modified since WP-008 (`git log --follow`, single commit `aa44cde`) and Stage 0 concluded with D-042 on 2026-09-13, a full day before D-073 (2026-09-14) changed the masking semantic this function depends on — so **D-042's own historical gate decision was computed under the masking semantic that was self-consistent AT THE TIME and is not retroactively corrupted by this finding.** What IS true: the code AS IT EXISTS TODAY would silently produce systematically day-shifted, wrong results if `jarvis stage0 probe` were ever run again under the current codebase — no test currently catches this (`_day_value` has no direct unit test; `test_probe_contexts.py` does not exercise the multi-day carried-forward-value scenario that would reveal the discrepancy). **Confirmed contained to `probe/`:** `describe/` (R1/R2/R5) does not import `probe/` and shares no code path with it — this finding has no bearing on Stage 2's reports. **Why this is not fixed here:** this package's explicit scope is infrastructure correctness that does not change what a report says about the data; this function feeds the arithmetic behind an already-closed, already-decided research gate (D-042), and while fixing it would not alter the historical D-042 record, deciding whether/how to re-verify Stage 0 with corrected code — or whether GBP/USD's confirmed status should be revisited on that basis at all — is a research judgment, not a mechanical one, and belongs to the user, not to an overnight infrastructure pass. Flagged here exactly as this package's own instructions require for anything crossing from mechanical fix into judgment call: stopped, written up, not guessed at | Active |
| D-084 | **Targeted coverage sweep (`pytest --cov`, installed transiently for analysis only, never added to `pyproject.toml`'s actual dependencies) across `describe/`+`reporting/` found and closed 10 genuinely-uncovered edge-value branches — all of the exact shape this package's own findings (D-077/D-078/D-079) came from: an empty-input case, a None-formatting placeholder, or a "no data survived" fallback path that every existing test fixture happened to route around** | **Found, not guessed at:** coverage before this entry's tests was 92% project-wide, 97-98% in the touched describe/reporting files specifically — the gaps were small in count but concentrated exactly where D-077/D-078/D-079 also found real issues, confirming coverage gaps and undiscovered edge-case bugs cluster together here, not a coincidence. **Ten branches closed, one per new/extended test:** `_extract_day_values`'s "day has bars but its own close isn't yet observable" case (r1.py, previously untested — distinct from "no bars this day at all"); `_stats_for`'s and `_bucket_stats`'s own n==0 branches (r1.py/r2.py, tested directly against the function's own contract regardless of whether today's call sites reach it); `_fmt_ci`/`_fmt_iqr`/`_fmt_pct_range`'s None-placeholder branches (all three renderers — every existing R2 fixture happened to produce only suppressed buckets, so the renderer's REAL-CI rendering path, and `_actual_bootstrap_params`'s by-year fallback, had literally never been exercised by any test despite being exactly what every real 2007-2014 run actually uses); `boxplot.py`'s `y_min==y_max` flat-data guard (present since R1 was built, never previously exercised — confirmed it genuinely prevents a division by zero rather than merely reading like it does). **Two confirmed DEAD, not tested, and not force-tested:** `compute_r1`'s and `compute_r2`'s own `if not days:` branches (r1.py:220, r2.py:205) are unreachable given `trading_day_boundaries`'s actual contract — it returns an empty day list if and only if `bars.height == 0`, which both callers already handle one line earlier via their own explicit `if bars.height == 0` check. Writing a test that pretended to reach this branch would have been misleading; reported honestly as defensive-but-currently-unreachable code instead. **One pre-existing, unrelated flaky test found by running the full suite under coverage instrumentation, not fixed (out of this package's scope):** `tests/unit/test_ingest_fetch.py::test_global_rate_limit_is_not_per_worker` failed once under `--cov` (a wall-clock elapsed-time assertion with a thin margin, tipped by coverage's own tracing overhead) and passed reliably (4/4) without coverage — a genuine, if narrow, test-fragility finding, unrelated to anything touched tonight, not one of this package's eight items, and not fixed here. **Verification:** 568 tests pass under both `pytest` and `python -m pytest` (up from 558 after D-077/D-078/D-079); 11/11 contracts held; `describe/`+`reporting/` combined coverage now 99% (625 statements, 6 genuinely uncovered: the 2 confirmed-dead branches above plus `furniture.py`'s `except OSError` git-unavailable path, judged out of the "division guard/suppression-threshold/empty-array" scope this sweep specifically targeted) | Active |
